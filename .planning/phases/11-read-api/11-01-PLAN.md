---
phase: 11-read-api
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/workers/api/routes/prompts.spec.ts
  - src/workers/api/routes/users.spec.ts
  - src/workers/api/routes/search.spec.ts
  - src/workers/api/routes/notifications.spec.ts
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-07
  - API-08
  - API-09
  - API-10
  - SEARCH-01
  - SEARCH-02

must_haves:
  truths:
    - "All spec files exist and every test is in a failing (RED) state when run against stub routes"
    - "Test files cover every API-01 through API-10 and SEARCH-01/SEARCH-02 behavior"
    - "Auth tiers are exercised: unauthenticated, optionalAuth, requireAuth"
    - "Error envelopes (400, 401, 404) are asserted in the correct shape"
  artifacts:
    - path: "src/workers/api/routes/prompts.spec.ts"
      provides: "Tests for GET /prompts, GET /prompts/:id, GET /prompts/:id/versions, GET /prompts/:id/comments"
    - path: "src/workers/api/routes/users.spec.ts"
      provides: "Tests for GET /users/:login, GET /users/:login/prompts, GET /users/:login/activity"
    - path: "src/workers/api/routes/search.spec.ts"
      provides: "Tests for GET /search, including FTS5 match, 400 on short/missing query"
    - path: "src/workers/api/routes/notifications.spec.ts"
      provides: "Tests for GET /notifications (requireAuth: 401 without cookie, 200 with)"
  key_links:
    - from: "spec files"
      to: "src/workers/api/index.ts"
      via: "import app from '../index'"
      pattern: "import app from"
    - from: "spec files"
      to: "cloudflare:test"
      via: "env, createExecutionContext, waitOnExecutionContext"
      pattern: "from 'cloudflare:test'"
---

<objective>
Write failing test scaffolds (RED phase) for all Phase 11 read endpoints before any implementation exists.

Purpose: Guarantees each endpoint has an explicit behavioral contract before code is written. Follows the Wave 0 pattern established in Phase 10 (auth.spec.ts).
Output: Four spec files covering 12 requirements; all tests fail against the current 501 stubs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-read-api/11-CONTEXT.md
@.planning/ROADMAP.md

<!-- Existing test pattern — follow exactly -->
@src/workers/api/routes/auth.spec.ts
@src/workers/api/index.spec.ts
@src/workers/api/test-setup.ts
@src/workers/api/wrangler.toml
</context>

<interfaces>
<!-- Patterns to replicate from auth.spec.ts -->

Test helper pattern (copy verbatim into each spec file):
```typescript
import { env, createExecutionContext, waitOnExecutionContext } from 'cloudflare:test'
import app from '../index'

async function request(req: Request, customEnv?: typeof env): Promise<Response> {
  const ctx = createExecutionContext()
  const res = await app.fetch(req, customEnv ?? env, ctx)
  await waitOnExecutionContext(ctx)
  return res
}
```

JWT mint helper (for authenticated requests):
```typescript
import { sign } from 'hono/jwt'
// jwtToken = await sign({ sub: '01DEVUSER000000000000000001', role: 'user', login: 'dev-user', exp: Math.floor(Date.now()/1000) + 86400 }, env.JWT_SECRET ?? 'test-secret')
```

Cursor/paginated response shape:
{ data: [...], next_cursor: string | null }

Error envelope shape:
{ error: string, code: string }

Seeded user IDs (from Phase 9 seed — hardcoded ULID-like prefixes):
- '01DEVUSER000000000000000001' (dev-user)
- '01DEVUSER000000000000000002' (dev-maintainer)
Seeded prompt IDs start with '01PROMPT...' prefix.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write prompts route test scaffold (RED)</name>
  <files>src/workers/api/routes/prompts.spec.ts</files>
  <behavior>
    - GET /prompts: returns 200 with { data: [...], next_cursor } shape (not 501)
    - GET /prompts?category=X: accepted without error
    - GET /prompts?limit=101: returns 400 { error: 'invalid_limit', code: 'invalid_limit' }
    - GET /prompts/:id with existing seed ID: returns 200 with id, title, body, author, tags, reaction_counts fields
    - GET /prompts/:id with nonexistent ID: returns 404 { error: 'not_found', code: 'not_found' }
    - GET /prompts/:id/versions with existing seed ID: returns 200 with { data: [...], next_cursor }
    - GET /prompts/:id/versions — each item has version_number, changelog, author.login, created_at fields
    - GET /prompts/:id/comments with existing seed ID: returns 200 with { data: [...], next_cursor }
    - GET /prompts/:id/comments — each item has id, body, author (or null for deleted), created_at fields
    - GET /prompts/:id unauthenticated: viewer_reaction and viewer_bookmarked fields NOT present
    - GET /prompts/:id with valid JWT cookie: viewer_reaction (null or string) and viewer_bookmarked (boolean) present
  </behavior>
  <action>
    Create src/workers/api/routes/prompts.spec.ts. Copy the request() and JWT mint helpers from auth.spec.ts. Write describe blocks for each route. Tests must reference real seeded IDs from the seed script (use '01PROMPT...' prefix pattern if exact ULIDs are unknown, or check seed SQL — Phase 9 seed uses hardcoded ULID-like prefixes). All tests will be RED against the current 501 stub.

    Use beforeAll to mint a jwtToken. In the optionalAuth tests, set a Cookie header with pc_session={jwtToken}.

    Do NOT mock D1 — tests run in the workerd runtime with the real in-memory D1 (same pattern as auth.spec.ts). The test-setup.ts beforeAll applies all migrations automatically.
  </action>
  <verify>
    <automated>cd src/workers/api && npx vitest run routes/prompts.spec.ts 2>&1 | grep -E "(FAIL|PASS|tests failed)" | head -20</automated>
  </verify>
  <done>prompts.spec.ts exists; running vitest shows all tests FAIL (not "file not found" or parse errors); no TypeScript errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write users + search + notifications test scaffolds (RED)</name>
  <files>src/workers/api/routes/users.spec.ts, src/workers/api/routes/search.spec.ts, src/workers/api/routes/notifications.spec.ts</files>
  <behavior>
    users.spec.ts:
    - GET /users/:login with seeded user login 'dev-user': returns 200 with login, name, avatar_url fields
    - GET /users/nonexistent-login: returns 404 { error: 'not_found', code: 'not_found' }
    - GET /users/:login/prompts: returns 200 { data: [...], next_cursor }
    - GET /users/:login/activity: returns 200 { data: [...], next_cursor }
    - GET /users/:login/activity — each item has type: 'prompt_created', prompt.id, prompt.title, prompt.created_at

    search.spec.ts:
    - GET /search with no q param: returns 400 { error: 'missing_query', code: 'missing_query' }
    - GET /search?q=a (1 char): returns 400 { error: 'query_too_short', code: 'query_too_short' }
    - GET /search?q=te (2 chars): returns 200 with { data: [...], next_cursor }
    - GET /search?q=prompt (known seed data word): returns 200 with data array (may be empty in fresh migration — just verify shape)
    - GET /search result items match the same shape as GET /prompts listing items

    notifications.spec.ts:
    - GET /notifications without cookie: returns 401 { error: 'unauthorized' }
    - GET /notifications with valid JWT cookie: returns 200 { data: [...], next_cursor }
    - GET /notifications items have id, type, read_at, created_at fields
  </behavior>
  <action>
    Create three spec files. Each imports app from '../index' and uses the request() helper pattern. All tests will be RED against current stubs (501 for users/search, 404 for /notifications which is not yet mounted).

    For notifications.spec.ts: GET /notifications will currently return 404 (not mounted). That is the expected failing state.

    For search.spec.ts: The FTS5 table and triggers already exist from Phase 9 migration (0002_fts5.sql). The seed data has at least one prompt — search for a word like 'example' or 'test' that likely appears in seed body/title.

    Follow the exact same import and helper pattern as auth.spec.ts.
  </action>
  <verify>
    <automated>cd src/workers/api && npx vitest run routes/users.spec.ts routes/search.spec.ts routes/notifications.spec.ts 2>&1 | grep -E "(FAIL|PASS|tests failed)" | head -20</automated>
  </verify>
  <done>All three spec files exist; vitest shows tests FAIL (RED state); no parse errors or import failures</done>
</task>

</tasks>

<verification>
Run all new spec files together:

```bash
cd src/workers/api && npx vitest run routes/prompts.spec.ts routes/users.spec.ts routes/search.spec.ts routes/notifications.spec.ts 2>&1 | tail -20
```

Expected: multiple test failures (RED state), zero TypeScript compile errors, zero "Cannot find module" errors.
</verification>

<success_criteria>
- 4 spec files created covering all 12 phase requirements (API-01..10, SEARCH-01, SEARCH-02)
- Every test is failing (RED) against current 501/404 stubs — proving they test real behavior
- Tests follow the cloudflare:test + workerd runtime pattern from auth.spec.ts (no mocked D1)
- Each spec file has at least 1 auth-tier test (unauthenticated, optionalAuth, or requireAuth)
- Error envelope shape { error, code } is asserted in at least one test per file
</success_criteria>

<output>
After completion, create `.planning/phases/11-read-api/11-01-SUMMARY.md`
</output>
