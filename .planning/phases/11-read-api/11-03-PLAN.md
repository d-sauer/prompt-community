---
phase: 11-read-api
plan: 03
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - src/workers/api/routes/users.ts
autonomous: true
requirements:
  - API-06
  - API-07
  - API-08

must_haves:
  truths:
    - "GET /users/:login returns public profile for a known user"
    - "GET /users/:login with unknown login returns 404 in standard error envelope"
    - "GET /users/:login/prompts returns the user's published submissions as a paginated list"
    - "GET /users/:login/activity returns a real feed of prompt_created events (not a placeholder)"
    - "All three user endpoints are public (no auth required)"
  artifacts:
    - path: "src/workers/api/routes/users.ts"
      provides: "Three GET handlers replacing the 501 stub"
      exports: ["default (Hono app)"]
  key_links:
    - from: "src/workers/api/routes/users.ts"
      to: "src/workers/api/db/schema.ts"
      via: "Drizzle queries against users, prompts, prompt_tags tables"
      pattern: "from '../db/schema'"
    - from: "GET /users/:login"
      to: "users.github_login column"
      via: "WHERE github_login = :login"
      pattern: "github_login"
---

<objective>
Implement three GET handlers in users.ts: public profile, user's prompts list, and user's activity feed.

Purpose: Turns the RED spec tests from Plan 01 GREEN; replaces the "coming soon" activity placeholder with real data (requirement API-08).
Output: src/workers/api/routes/users.ts with real Drizzle implementations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-read-api/11-CONTEXT.md

@src/workers/api/db/schema.ts
@src/workers/api/index.ts
@src/workers/api/routes/users.spec.ts
</context>

<interfaces>
<!-- Schema fields used by users routes -->

users table: id, github_login (used as public 'login'), name, avatar_url, role, created_at
prompts table: id, author_id, title, category, model, difficulty, status, created_at, updated_at
prompt_tags table: prompt_id, tag

Env type:
```typescript
type Env = {
  Bindings: { DB: D1Database }
  Variables: { user: UserContext | null }
}
```

Drizzle init (same as Plan 02):
```typescript
import { drizzle } from 'drizzle-orm/d1'
import * as schema from '../db/schema'
const db = drizzle(c.env.DB, { schema })
```

Activity feed shape (from CONTEXT.md):
```typescript
{ type: 'prompt_created', prompt: { id, title, created_at } }
```
Derived from: prompts WHERE author_id = user.id AND status = 'published', ORDER BY id DESC.
No separate activity_log table — query prompts directly.

User prompts shape: same PromptSummary shape as GET /prompts (minus viewer fields — no auth on this endpoint).
Only published prompts returned.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GET /users/:login (public profile)</name>
  <files>src/workers/api/routes/users.ts</files>
  <behavior>
    - GET /users/:login: returns 200 with { id, login, name, avatar_url, role, created_at }
    - login param matches users.github_login column
    - Unknown login: returns 404 { error: 'not_found', code: 'not_found' }
    - No auth required
  </behavior>
  <action>
    Replace the 501 stub in users.ts with a real GET /:login handler.

    Query: `db.select().from(schema.users).where(eq(schema.users.github_login, login)).limit(1)`

    Map result: return { id: user.id, login: user.github_login, name: user.name, avatar_url: user.avatar_url, role: user.role, created_at: user.created_at }.

    If no user found, return c.json({ error: 'not_found', code: 'not_found' }, 404).
  </action>
  <verify>
    <automated>cd src/workers/api && npx vitest run routes/users.spec.ts 2>&1 | grep -E "(GET /users/:login|✓|✗)" | head -10</automated>
  </verify>
  <done>GET /users/:login tests in users.spec.ts pass; 404 on unknown login works</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GET /users/:login/prompts and GET /users/:login/activity</name>
  <files>src/workers/api/routes/users.ts</files>
  <behavior>
    GET /users/:login/prompts:
    - Returns { data: PromptSummary[], next_cursor }
    - Only published prompts (status = 'published')
    - Cursor pagination: WHERE id < :cursor ORDER BY id DESC LIMIT :limit (default 20, max 100)
    - limit > 100: 400 { error: 'invalid_limit', code: 'invalid_limit' }
    - Unknown login: 404
    - PromptSummary includes: id, title, category, model, difficulty, status, tags: string[], author: { login, avatar_url }, reaction_counts: { thumbs_up, heart, rocket }, comment_count, created_at, updated_at (no viewer fields — unauthenticated endpoint)

    GET /users/:login/activity:
    - Returns { data: ActivityEvent[], next_cursor }
    - Each ActivityEvent: { type: 'prompt_created', prompt: { id, title, created_at } }
    - Derived from prompts WHERE author_id = user.id AND status = 'published' ORDER BY id DESC
    - Cursor pagination same as above
    - Unknown login: 404
    - No auth required
  </behavior>
  <action>
    Add two more handlers to users.ts (append after the GET /:login handler from Task 1).

    For GET /:login/prompts: first resolve user by github_login (404 if not found), then query prompts with author_id = user.id. For tags and reaction_counts: use the same in-JS aggregation pattern as Plan 02 (fetch prompt_tags rows where prompt_id IN [...], fetch reactions rows, aggregate in JS).

    For GET /:login/activity: resolve user by github_login (404 if not found), then `db.select({ id: prompts.id, title: prompts.title, created_at: prompts.created_at }).from(schema.prompts).where(and(eq(schema.prompts.author_id, user.id), eq(schema.prompts.status, 'published')))...`. Map each row to `{ type: 'prompt_created', prompt: { id, title, created_at } }`.

    Pagination uses same ULID cursor pattern (WHERE id < :cursor ORDER BY id DESC for activity feed).
  </action>
  <verify>
    <automated>cd src/workers/api && npx vitest run routes/users.spec.ts 2>&1 | grep -E "(✓|✗|PASS|FAIL|tests)" | tail -10</automated>
  </verify>
  <done>All tests in users.spec.ts pass (GREEN); TypeScript compiles without errors for users.ts</done>
</task>

</tasks>

<verification>
```bash
cd src/workers/api && npx vitest run routes/users.spec.ts
```
All GREEN. Then:
```bash
npx tsc --project tsconfig.worker.json --noEmit 2>&1 | head -20
```
No errors.
</verification>

<success_criteria>
- All users.spec.ts tests pass
- GET /users/:login: real profile from D1, 404 on unknown login
- GET /users/:login/prompts: paginated published prompts only
- GET /users/:login/activity: real prompt_created events, not a placeholder
- All endpoints require no authentication (public)
- No TypeScript compile errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-read-api/11-03-SUMMARY.md`
</output>
