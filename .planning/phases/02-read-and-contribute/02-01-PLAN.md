---
phase: 02-read-and-contribute
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/lib/search.ts
  - src/lib/markdown.ts
  - src/lib/frontmatter.ts
  - src/lib/github/queries.ts
  - src/lib/github/etag.ts
  - src/composables/queries/usePromptsQuery.ts
  - src/composables/queries/usePromptDetail.ts
  - src/stores/useSearchStore.ts
  - src/stores/usePromptsStore.ts
  - src/stores/useUIStore.ts
  - src/views/BrowseView.vue
  - src/views/PromptDetailView.vue
  - src/components/browse/PromptList.vue
  - src/components/browse/PromptRow.vue
  - src/components/browse/SkeletonRow.vue
  - src/components/browse/EmptyState.vue
  - src/components/browse/FilterChips.vue
  - src/components/prompt/PromptDetail.vue
  - src/components/prompt/PromptMetadata.vue
  - src/components/prompt/MarkdownBody.vue
  - src/components/prompt/PromptActions.vue
  - src/App.vue
  - src/lib/search.spec.ts
  - src/lib/markdown.spec.ts
  - src/lib/frontmatter.spec.ts
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
  - DISC-06
  - DISC-07
  - DISC-08
  - DISC-09
  - DISC-10
  - INFR-01
  - INFR-02
  - INFR-03
  - INFR-05
  - INFR-06

must_haves:
  truths:
    - "Any user can open the app without logging in and see a paginated list of prompts load within 2s"
    - "Any user can filter the list by category (Engineering/Business/General) and AI model; the URL-active sidebar filters reflect the selection"
    - "Any user can sort the list by Most Voted, Newest, or Most Commented"
    - "Any user can type in the search box and see filtered results appear in under 50ms without any network request"
    - "Any user can click a prompt row and see its full markdown content rendered with syntax-highlighted code blocks"
    - "Any user can copy a prompt's full content to clipboard with a single click and see a toast confirmation"
    - "Any user can share a permalink URL to a specific prompt that opens directly to that prompt's detail"
    - "Infinite scroll triggers at list bottom and loads the next page automatically with skeleton loading placeholders"
    - "Repeat requests to GitHub use ETags; second request returns 304 and costs zero API rate-limit points"
    - "MiniSearch search never calls GitHub Search API — all filtering is local"
  artifacts:
    - path: "src/lib/search.ts"
      provides: "createSearchIndex(prompts), searchPrompts(index, query) — MiniSearch inverted index"
      exports: ["createSearchIndex", "searchPrompts"]
    - path: "src/lib/markdown.ts"
      provides: "initMarkdown() async singleton, renderMarkdown(md, content) — markdown-it + Shiki"
      exports: ["initMarkdown", "renderMarkdown"]
    - path: "src/lib/frontmatter.ts"
      provides: "parseFrontmatter(body), buildFrontmatter(meta) — YAML round-trip"
      exports: ["parseFrontmatter", "buildFrontmatter", "PromptFrontmatter"]
    - path: "src/lib/github/queries.ts"
      provides: "GET_PROMPTS and GET_PROMPT_DETAIL GraphQL query strings"
    - path: "src/lib/github/etag.ts"
      provides: "ETag cache Map and withETag fetch wrapper for conditional requests"
    - path: "src/composables/queries/usePromptsQuery.ts"
      provides: "usePromptsQuery(filters) — TanStack useInfiniteQuery with cursor pagination"
      exports: ["usePromptsQuery"]
    - path: "src/composables/queries/usePromptDetail.ts"
      provides: "usePromptDetail(id) — single prompt useQuery"
      exports: ["usePromptDetail"]
    - path: "src/stores/useSearchStore.ts"
      provides: "Extended: buildIndex(), search(), MiniSearch instance, results computed"
    - path: "src/views/BrowseView.vue"
      provides: "Full browse split-pane: PromptList (left) + PromptDetail (right)"
    - path: "src/views/PromptDetailView.vue"
      provides: "Standalone detail view for direct URL navigation /prompts/:id"
  key_links:
    - from: "src/stores/useSearchStore.ts"
      to: "src/lib/search.ts"
      via: "buildIndex calls createSearchIndex; results computed calls searchPrompts"
      pattern: "createSearchIndex|searchPrompts"
    - from: "src/composables/queries/usePromptsQuery.ts"
      to: "src/lib/github/octokit.ts"
      via: "createGraphqlClient(authStore.token) for anonymous + authenticated access"
      pattern: "createGraphqlClient"
    - from: "src/views/BrowseView.vue"
      to: "src/stores/usePromptsStore.ts"
      via: "filterState and sortOrder from store drive usePromptsQuery queryKey"
      pattern: "usePromptsStore"
    - from: "src/components/prompt/MarkdownBody.vue"
      to: "src/lib/markdown.ts"
      via: "renderMarkdown(mdInstance, content) called after v-if markdownReady"
      pattern: "renderMarkdown|markdownReady"
    - from: "src/App.vue"
      to: "src/lib/markdown.ts"
      via: "initMarkdown() called in onMounted; sets useUIStore.markdownReady = true"
      pattern: "initMarkdown|markdownReady"
---

<objective>
Build the complete read/discovery layer: GitHub GraphQL data queries, MiniSearch client-side search index, markdown rendering with Shiki syntax highlighting, ETag caching, and the browse + prompt detail UI screens.

Purpose: This is the foundation all other Phase 2 plans depend on. Plans 02-02 and 02-03 both need usePromptDetail and the data contracts defined here. This plan delivers the core value proposition — any user can discover and read prompts without logging in.

Output: Working BrowseView with filter/sort/infinite scroll/search, PromptDetailView with rendered markdown, all lib utilities (search, markdown, frontmatter, etag), and composable queries.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-read-and-contribute/02-RESEARCH.md
@.planning/phases/02-read-and-contribute/02-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Key contracts from Phase 1 that this plan builds on. -->

From src/types/index.ts (extend — do NOT replace existing types):
```typescript
// Existing Phase 1 types to keep: GitHubUser, FilterState, SortOrder, AuthState
// Add in Phase 2:
export interface Prompt {
  id: number           // GitHub issue number
  title: string
  body: string         // raw markdown content (after frontmatter stripped)
  frontmatter: PromptFrontmatter
  author: { login: string; avatarUrl: string }
  createdAt: string
  updatedAt: string
  labels: Array<{ name: string; color: string }>
  reactionGroups: Array<{ content: string; reactors: { totalCount: number } }>
  commentCount: number
}

export interface VersionObject {
  version: number
  date: Date
  changelog: string
  content: string
  commentId: string
  author: string
  authorAvatar: string
}

export type ContentType = 'prompt' | 'skill-file' | 'skill-set'
```

From src/stores/usePromptsStore.ts (extend existing store):
```typescript
// Existing: filterState (FilterState), sortOrder (SortOrder)
// Add: selectedPromptId: ref<number | null>(null)
```

From src/stores/useUIStore.ts (extend existing store):
```typescript
// Existing: sidebarCollapsed, notificationDrawerOpen, commandPaletteOpen, sidebarMobileOpen
// Add: markdownReady: ref(false), diffLayout: ref<'side-by-side' | 'unified'>('side-by-side')
```

From src/lib/github/octokit.ts:
```typescript
export function createGraphqlClient(token?: string): ReturnType<typeof graphql.defaults>
export function createRestClient(token: string): Octokit
```

From src/stores/useAuthStore.ts:
```typescript
// authStore.token: string | null  (memory-only ref)
// authStore.isAuthenticated: boolean
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test stubs + lib utilities (search, markdown, frontmatter, etag)</name>
  <files>
    src/lib/search.spec.ts,
    src/lib/markdown.spec.ts,
    src/lib/frontmatter.spec.ts,
    src/lib/search.ts,
    src/lib/markdown.ts,
    src/lib/frontmatter.ts,
    src/lib/github/etag.ts,
    src/lib/github/queries.ts,
    src/types/index.ts
  </files>
  <behavior>
    Wave 0 test stubs (RED — failing, but recognized by Vitest):
    - src/lib/search.spec.ts: it.todo for "builds index from prompts array", "search returns results in < 50ms", "empty query returns all prompts", "search with no index returns empty array"
    - src/lib/markdown.spec.ts: it.todo for "initMarkdown returns MarkdownIt instance", "renderMarkdown renders code block with shiki class", "renders without XSS (html: false)"
    - src/lib/frontmatter.spec.ts: it.todo for "buildFrontmatter round-trips through parseFrontmatter", "parseFrontmatter handles missing frontmatter", "tags with colons serialize correctly"

    Implementation (GREEN):
    - src/lib/search.ts: createSearchIndex(prompts: Prompt[]) using MiniSearch with fields ['title', 'body', 'tags'], storeFields ['id','title','author','category','model','difficulty','tags'], searchOptions {boost:{title:3,tags:2,body:1}, prefix:true, fuzzy:0.2}. searchPrompts(index, query) returns [] for empty query.
    - src/lib/markdown.ts: initMarkdown() async singleton — createHighlighter({themes:['github-dark'], langs:[10 langs]}), new MarkdownIt({html:false, linkify:true, typographer:true}), mdInstance.use(fromHighlighter(highlighter, {theme:'github-dark'})). renderMarkdown(md, content) calls md.render(content).
    - src/lib/frontmatter.ts: PromptFrontmatter interface (type, category, model, difficulty, tags, version, changelog?). buildFrontmatter(meta) returns `---\n${YAML.stringify(meta)}---\n\n`. parseFrontmatter(body) regex match /^---\r?\n([\s\S]*?)\r?\n---\r?\n?([\s\S]*)$/.
    - src/lib/github/etag.ts: Map<string, string> etagCache module-level. getEtag(key), setEtag(key, value), clearEtag(key) exported functions. Also export etagFetchWrapper(url, options, cacheKey) that injects If-None-Match header from cache, stores ETag from response headers into cache.
    - src/lib/github/queries.ts: Export GET_PROMPTS (20 issues, cursor pagination, labels filter, reactionGroups, comments.totalCount) and GET_PROMPT_DETAIL (single issue by number, all comments for version parsing) as string constants.
    - src/types/index.ts: Add Prompt, VersionObject, ContentType, DraftState interfaces (do NOT remove existing Phase 1 types).
  </behavior>
  <action>
    First write Wave 0 spec stubs using it.todo() so Vitest recognizes them. Then implement each lib file.

    Install new Phase 2 dependencies before writing lib files:
    ```bash
    npm install markdown-it @shikijs/markdown-it shiki diff
    npm install -D @types/markdown-it @types/diff
    npx shadcn-vue@latest add input textarea select scroll-area resizable tabs toast skeleton card
    ```

    Key implementation details:
    - markdown.ts: Shiki createHighlighter is async — export initMarkdown() that initializes once and caches in module-level `let mdInstance`. Export renderMarkdown(md, content) as pure function (no async). This lets App.vue call initMarkdown() in onMounted and then pass the instance down.
    - frontmatter.ts: Use `yaml` package (already installed). Import as `import YAML from 'yaml'`. Use YAML.stringify() — never string concatenation — to handle colon-in-value edge cases.
    - etag.ts: The etagFetchWrapper is lightweight — it wraps a fetch call with If-None-Match injection and ETag storage. GitHub GraphQL ETags work at the transport layer; this satisfies INFR-05.
    - types/index.ts: Keep all Phase 1 interfaces (GitHubUser, FilterState, SortOrder, AuthState). Add Prompt with id as GitHub issue number.
  </action>
  <verify>
    <automated>npx vitest run src/lib/search.spec.ts src/lib/markdown.spec.ts src/lib/frontmatter.spec.ts</automated>
  </verify>
  <done>All 3 spec files pass (todo stubs recognized + any implemented tests green). TypeScript build zero errors. markdown-it, shiki, diff installed. shadcn-vue components added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Data composables (usePromptsQuery, usePromptDetail, useSearchStore extension, usePromptsStore extension)</name>
  <files>
    src/composables/queries/usePromptsQuery.ts,
    src/composables/queries/usePromptDetail.ts,
    src/stores/useSearchStore.ts,
    src/stores/usePromptsStore.ts,
    src/stores/useUIStore.ts,
    src/App.vue
  </files>
  <behavior>
    usePromptsQuery: TanStack useInfiniteQuery; queryKey ['prompts', filters]; queryFn calls createGraphqlClient(token ?? undefined) with GET_PROMPTS query; getNextPageParam uses pageInfo.endCursor; staleTime 60_000; initialPageParam undefined.
    usePromptDetail: TanStack useQuery; queryKey ['prompt', id]; queryFn calls createGraphqlClient(token) with GET_PROMPT_DETAIL; staleTime 300_000; enabled only when id is truthy.
    useSearchStore: Extend existing store — add buildIndex(prompts: Prompt[]), miniSearch instance (non-reactive module-level variable), extended results computed that calls searchPrompts if query is set. Keep existing query and setQuery.
    usePromptsStore: Add selectedPromptId: ref<number | null>(null) and setSelectedPrompt(id) action.
    useUIStore: Add markdownReady: ref(false) and diffLayout: ref<'side-by-side' | 'unified'>('side-by-side').
    App.vue: Call initMarkdown() in onMounted, then set uiStore.markdownReady = true. Import useUIStore.
  </behavior>
  <action>
    Create src/composables/queries/ directory. Implement usePromptsQuery.ts and usePromptDetail.ts.

    usePromptsQuery implementation note: The queryFn must flatten pages for MiniSearch. After usePromptsQuery returns, BrowseView watches allPrompts (flattened from all pages) and calls searchStore.buildIndex(allPrompts) when data changes. The queryKey includes filters so filter changes trigger fresh queries.

    useSearchStore extension: The MiniSearch instance is stored in a module-level `let miniSearchInstance: MiniSearch | null = null` (not a ref) because MiniSearch objects are not reactive-friendly. The results computed ref reads query.value, and if it is non-empty and miniSearchInstance exists, returns searchPrompts(miniSearchInstance, query.value). Otherwise returns allPrompts.value.

    App.vue onMounted: `const md = await initMarkdown(); uiStore.markdownReady = true; // store md instance in useUIStore or module singleton`. The MarkdownIt instance can be stored in a module-level variable in markdown.ts (already handled by the singleton pattern) — App.vue just needs to trigger init and flip the ready flag.
  </action>
  <verify>
    <automated>npx vitest run src/stores/ src/composables/</automated>
  </verify>
  <done>usePromptsQuery and usePromptDetail exist and TypeScript compiles. useSearchStore.buildIndex() and useSearchStore.results computed work. usePromptsStore.selectedPromptId exists. useUIStore.markdownReady exists. App.vue calls initMarkdown() in onMounted.</done>
</task>

<task type="auto">
  <name>Task 3: BrowseView, PromptDetailView, and browse/prompt components</name>
  <files>
    src/views/BrowseView.vue,
    src/views/PromptDetailView.vue,
    src/components/browse/PromptList.vue,
    src/components/browse/PromptRow.vue,
    src/components/browse/SkeletonRow.vue,
    src/components/browse/EmptyState.vue,
    src/components/browse/FilterChips.vue,
    src/components/prompt/PromptDetail.vue,
    src/components/prompt/PromptMetadata.vue,
    src/components/prompt/MarkdownBody.vue,
    src/components/prompt/PromptActions.vue
  </files>
  <action>
    BrowseView.vue: ResizablePanelGroup split (left: PromptList, right: PromptDetail). Left panel shows FilterChips at top, then PromptList. Right panel shows PromptDetail when selectedPromptId is set, otherwise empty state. On mobile (< lg), show only list; clicking a prompt navigates to /prompts/:id standalone view. Watch usePromptsQuery.data and call searchStore.buildIndex() on data change. Attach useIntersectionObserver (VueUse) on a sentinel div at list bottom to call fetchNextPage().

    PromptList.vue: Props: prompts Prompt[], isLoading boolean, hasNextPage boolean. Show 5 SkeletonRow when isLoading. Map prompts to PromptRow. Show EmptyState when not loading and prompts.length === 0.

    PromptRow.vue: Props: prompt Prompt. Shows title, author avatar+login, category badge (from frontmatter.category label), vote count (sum of 👍 reactionGroup), comment count, createdAt relative date. Click sets usePromptsStore.selectedPromptId. Active row highlighted (purple left border + background — same pattern as Sidebar active state from Phase 1).

    SkeletonRow.vue: 5 skeleton rows with animate-pulse using shadcn Skeleton component. Matches DISC-10 requirement for skeleton loading states.

    FilterChips.vue: Renders active category and model filters as dismissible chips. Reads usePromptsStore.filterState. Click on chip clears that filter.

    EmptyState.vue: "No prompts found" message with clear-filters action button.

    PromptDetail.vue: Props: promptId number. Uses usePromptDetail(promptId). Shows PromptMetadata above, MarkdownBody below, PromptActions (copy, share) at top right. v-if="uiStore.markdownReady" before rendering MarkdownBody.

    PromptMetadata.vue: Props: prompt Prompt. Shows all DISC-07 fields: content type badge, category, AI model, difficulty, vote count, comment count, up to 5 tags, author login + avatar, createdAt date. Use shadcn Badge for labels.

    MarkdownBody.vue: Props: content string. Uses renderMarkdown(mdInstance, content) where mdInstance comes from the initMarkdown() singleton (import directly from markdown.ts). v-html binding on rendered HTML. v-if="uiStore.markdownReady" guard.

    PromptActions.vue: Props: prompt Prompt. Copy button using VueUse useClipboard() — copies prompt.body (markdown content without frontmatter). On copy success, show sonner toast "Copied to clipboard". Share button copies window.location.href with /prompts/:id path via useClipboard(). Both satisfy DISC-08 and DISC-09.

    PromptDetailView.vue (standalone): Used when navigating directly to /prompts/:id. Uses useRoute().params.id, calls usePromptDetail. Renders PromptDetail component. This view has no ResizablePanelGroup — it is the full-width standalone version.

    Router: Ensure /prompts/:id route maps to PromptDetailView and /prompts/:id/versions maps to VersionHistoryView (stub for Plan 02-03). Both routes should already be in the router from Phase 1 stubs — just confirm they are wired to the correct view components.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build succeeds with zero TypeScript errors. BrowseView renders with ResizablePanelGroup split pane. PromptRow shows title + metadata. SkeletonRow shows during load. MarkdownBody renders markdown with syntax highlighting. Copy button triggers clipboard. Full test suite green.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npx vitest run src/lib/search.spec.ts src/lib/markdown.spec.ts src/lib/frontmatter.spec.ts` — all pass
2. `npm run build` — zero TypeScript errors, dist/ produced
3. `npx vitest run --coverage` — full suite green
4. Manual: Open app, browse view loads with prompt list (or empty state if no GitHub data in .env). Filter sidebar chips update the list. Search box filters instantly. Click prompt to see split-pane detail. Copy button copies content.
</verification>

<success_criteria>
- DISC-01: BrowseView loads without auth (createGraphqlClient(undefined) for anonymous access)
- DISC-02/03: Category and AI model filters passed as label filter to GraphQL query
- DISC-04: Sort by Most Voted (client-side reaction count sort), Newest (GraphQL orderBy CREATED_AT), Most Commented (client-side comment count sort)
- DISC-05/INFR-03/INFR-06: MiniSearch index built from prompts; search returns results in < 50ms; no GitHub Search API calls
- DISC-06: MarkdownBody renders code blocks with Shiki syntax highlighting (html: false, no XSS)
- DISC-07: PromptMetadata shows all 8 metadata fields
- DISC-08: Copy button uses VueUse useClipboard(); toast confirmation shown
- DISC-09: Share button copies permalink URL to clipboard
- DISC-10: useIntersectionObserver sentinel triggers fetchNextPage(); 5 skeleton rows show during load
- INFR-01: Shiki loaded as async singleton — does not block FCP
- INFR-02: Single GraphQL query for 20 issues; TanStack Query staleTime 60s
- INFR-05: etagFetchWrapper injects If-None-Match; stores ETag from response
</success_criteria>

<output>
After completion, create `.planning/phases/02-read-and-contribute/02-01-SUMMARY.md` following the summary template.
</output>
