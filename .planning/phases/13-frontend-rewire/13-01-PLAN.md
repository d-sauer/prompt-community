---
phase: 13-frontend-rewire
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/stores/useBookmarksStore.ts
  - src/lib/api/http.ts
  - src/lib/api/queries.ts
  - src/lib/api/mutations.ts
  - src/lib/api/http.spec.ts
  - src/lib/api/queries.spec.ts
  - src/lib/api/mutations.spec.ts
autonomous: true
requirements:
  - FRONT-01
  - FRONT-02
  - FRONT-03
  - FRONT-04
  - FRONT-05

must_haves:
  truths:
    - "apiFetch injects credentials: 'include' and base URL from VITE_API_URL on every call"
    - "apiFetch throws an Error with the API error message on non-2xx responses"
    - "apiFetch returns {} as T on 204 No Content responses"
    - "src/lib/api/queries.ts exports typed functions for every GET endpoint (prompts, users, search, labels, notifications)"
    - "src/lib/api/mutations.ts exports typed functions for every write endpoint (prompts, comments, reactions, bookmarks, notifications); no token parameter on any function"
    - "Prompt.id is string (ULID); nodeId field is removed from the Prompt interface"
    - "useBookmarksStore bookmarkedIds is string[] not number[]"
    - "No src/lib/api/admin.ts is created (Phase 14 scope per CONTEXT.md locked decision)"
  artifacts:
    - path: src/types/index.ts
      provides: "Updated Prompt interface: id is string, nodeId removed"
    - path: src/stores/useBookmarksStore.ts
      provides: "bookmarkedIds: string[] (was number[])"
    - path: src/lib/api/http.ts
      provides: "apiFetch<T> function"
      exports: ["apiFetch"]
    - path: src/lib/api/queries.ts
      provides: "All GET API functions"
      exports:
        - getPrompts
        - getPromptDetail
        - getPromptVersions
        - getPromptComments
        - searchPrompts
        - getLabels
        - getNotifications
        - getUserProfile
        - getUserPrompts
        - getUserActivity
    - path: src/lib/api/mutations.ts
      provides: "All write API functions (no token params)"
      exports:
        - createPrompt
        - updatePrompt
        - deletePrompt
        - createVersion
        - restoreVersion
        - postComment
        - deleteComment
        - addReaction
        - removeReaction
        - addBookmark
        - removeBookmark
        - markNotificationRead
        - flagPrompt
    - path: src/lib/api/http.spec.ts
      provides: "Unit tests for apiFetch (6 behaviors)"
    - path: src/lib/api/queries.spec.ts
      provides: "Unit tests verifying each exported GET function calls apiFetch with the correct path"
    - path: src/lib/api/mutations.spec.ts
      provides: "Unit tests verifying each exported write function calls apiFetch with the correct path and method"
  key_links:
    - from: src/lib/api/queries.ts
      to: src/lib/api/http.ts
      via: "import { apiFetch } from './http'"
      pattern: "apiFetch.*get"
    - from: src/lib/api/mutations.ts
      to: src/lib/api/http.ts
      via: "import { apiFetch } from './http'"
      pattern: "apiFetch.*post|patch|delete"
---

<objective>
Build the type foundation and complete API client layer for Phase 13.

Purpose: Composable migration (Plans 03 and 04) cannot start until the API client functions exist and the Prompt type is updated. This plan creates the contracts every downstream plan builds against.

Output:
- Updated type contracts in `src/types/index.ts` and `src/stores/useBookmarksStore.ts`
- `src/lib/api/http.ts` — minimal `apiFetch<T>` fetch wrapper
- `src/lib/api/queries.ts` — all GET API functions
- `src/lib/api/mutations.ts` — all write API functions (no token parameters)
- `src/lib/api/http.spec.ts`, `queries.spec.ts`, `mutations.spec.ts` — unit test coverage for the API client layer
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-frontend-rewire/13-CONTEXT.md
@.planning/phases/13-frontend-rewire/13-RESEARCH.md
</context>

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From src/types/index.ts (current — MUST be updated):
```typescript
export interface Prompt {
  id: number           // CHANGE TO: id: string (ULID)
  nodeId: string       // REMOVE — no GraphQL node ID in v2 API
  title: string
  body: string
  frontmatter: PromptFrontmatter
  author: { login: string; avatarUrl: string }
  createdAt: string
  updatedAt: string
  labels: Array<{ name: string; color: string }>
  reactionGroups: ReactionGroup[]
  commentCount: number
  comments: CommentNode[]
}
```

From src/workers/api/routes (backend — for matching signatures):
- GET /prompts — query: category, model, difficulty, sort, cursor, limit → { data: Prompt[], next_cursor: string | null }
- GET /prompts/:id — returns single Prompt with comments embedded or comment_count
- GET /prompts/:id/versions — returns VersionObject[]
- GET /prompts/:id/comments — returns { data: CommentNode[], next_cursor }
- GET /search?q= — returns { data: Prompt[], next_cursor }
- GET /labels — returns labels grouped by type
- GET /notifications — returns { data: Notification[], next_cursor }
- GET /users/:login — returns { login, name, avatar_url, role, ... }
- GET /users/:login/prompts — returns { data: Prompt[], next_cursor }
- GET /users/:login/activity — returns { data: ActivityItem[], next_cursor }

Pagination shape (Phase 11 decision):
```typescript
interface PageResponse<T> {
  data: T[]
  next_cursor: string | null
}
```

Activity item shape:
```typescript
interface ActivityItem {
  type: 'prompt_created'
  prompt: { id: string; title: string; created_at: string }
}
```

FilterState (src/types/index.ts):
```typescript
export interface FilterState {
  category: string | null
  model: string | null
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update type contracts — Prompt.id to string, drop nodeId, bookmarks to string[]</name>
  <files>src/types/index.ts, src/stores/useBookmarksStore.ts</files>
  <action>
In `src/types/index.ts`:
- Change `Prompt.id` from `number` to `string` (ULID from API)
- Remove `nodeId: string` field from the Prompt interface — the v2 API has no GraphQL node ID concept
- Keep all other fields unchanged
- Add `status?: 'draft' | 'published' | 'flagged' | 'hidden'` field to Prompt (the API returns this)
- Keep `AuthState`, `GitHubUser`, and all other types as-is — do not modify anything outside the Prompt interface and nodeId removal

In `src/stores/useBookmarksStore.ts`:
- Change `bookmarkedIds = useLocalStorage<number[]>('bookmarks', [])` to `useLocalStorage<string[]>('bookmarks', [])`
- Change `isBookmarked = (id: number)` to `(id: string)`
- Change `toggle(id: number)` to `toggle(id: string)`
- The localStorage key stays as 'bookmarks' — the stored value type changes from number to string (ULID)

TypeScript will cascade errors to consuming components after this change — that is expected. Plans 03, 04, and 05 resolve those errors. Run `npx tsc --noEmit` after this task to see the full error list (do not fix errors here — they are resolved in subsequent plans).
  </action>
  <verify>
    <automated>grep -n "id: string\|nodeId" /Users/davor/development/repo/d-sauer/prompt-community/src/types/index.ts && grep -n "string\[\]" /Users/davor/development/repo/d-sauer/prompt-community/src/stores/useBookmarksStore.ts</automated>
  </verify>
  <done>Prompt.id is `string`, nodeId field is absent from Prompt interface, useBookmarksStore uses string[] for bookmarkedIds</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/lib/api/http.ts, queries.ts, mutations.ts — with full spec coverage</name>
  <files>
    src/lib/api/http.ts,
    src/lib/api/queries.ts,
    src/lib/api/mutations.ts,
    src/lib/api/http.spec.ts,
    src/lib/api/queries.spec.ts,
    src/lib/api/mutations.spec.ts
  </files>
  <behavior>
    http.spec.ts behaviors:
    - Test 1: apiFetch includes `credentials: 'include'` in every request
    - Test 2: apiFetch prepends VITE_API_URL (strips trailing slash) to path
    - Test 3: apiFetch throws Error with message from `body.error` on 4xx response
    - Test 4: apiFetch throws Error with `HTTP 500` message when body has no error field on 5xx response
    - Test 5: apiFetch returns `{}` for 204 No Content responses
    - Test 6: apiFetch returns parsed JSON for 200 responses

    queries.spec.ts behaviors (one test per exported function):
    - Each exported function calls apiFetch with the correct path prefix (e.g. getPrompts calls '/prompts', getUserProfile calls '/users/{login}')
    - Cursor and filter params are serialized as query string when provided

    mutations.spec.ts behaviors (one test per exported function):
    - Each exported function calls apiFetch with the correct path and HTTP method (POST/PATCH/DELETE)
    - No function accepts a token parameter — verify function signatures
  </behavior>
  <action>
Create `src/lib/api/http.ts` using the canonical implementation from RESEARCH.md:

```typescript
export async function apiFetch<T = unknown>(
  path: string,
  options: RequestInit = {},
): Promise<T> {
  const base = (import.meta.env.VITE_API_URL as string).replace(/\/$/, '')
  const res = await fetch(`${base}${path}`, {
    ...options,
    credentials: 'include',
    headers: {
      'Content-Type': 'application/json',
      ...((options.headers as Record<string, string>) ?? {}),
    },
  })
  if (!res.ok) {
    let message = `HTTP ${res.status}`
    try {
      const body = await res.json() as { error?: string }
      if (body.error) message = body.error
    } catch { /* ignore parse errors */ }
    throw new Error(message)
  }
  if (res.status === 204) return {} as T
  return res.json() as Promise<T>
}
```

Create `src/lib/api/queries.ts` — import apiFetch, export all GET functions:

Functions to implement (match backend route signatures exactly):
- `getPrompts(params: FilterState & { cursor?: string; limit?: number; sort?: string })` → `apiFetch('/prompts?' + URLSearchParams)` → `PageResponse<Prompt>`
- `getPromptDetail(id: string)` → `apiFetch(\`/prompts/${id}\`)` → `Prompt`
- `getPromptVersions(id: string)` → `apiFetch(\`/prompts/${id}/versions\`)` → `VersionObject[]`
- `getPromptComments(id: string, params?: { cursor?: string; limit?: number })` → `apiFetch(\`/prompts/${id}/comments\`)` → `PageResponse<CommentNode>`
- `searchPrompts(q: string, params?: { cursor?: string; limit?: number })` → `apiFetch('/search?' + URLSearchParams)` → `PageResponse<Prompt>`
- `getLabels()` → `apiFetch('/labels')` → labels response
- `getNotifications(params?: { cursor?: string; limit?: number })` → `apiFetch('/notifications')` → `PageResponse<NotificationItem>`
- `getUserProfile(login: string)` → `apiFetch(\`/users/${login}\`)` → user profile
- `getUserPrompts(login: string, params?: { cursor?: string; limit?: number })` → `apiFetch(\`/users/${login}/prompts\`)` → `PageResponse<Prompt>`
- `getUserActivity(login: string, cursor?: string)` → `apiFetch(\`/users/${login}/activity?...\`)` → `PageResponse<ActivityItem>`

Define the ActivityItem interface in this file:
```typescript
export interface ActivityItem {
  type: 'prompt_created'
  prompt: { id: string; title: string; created_at: string }
}
```

Define NotificationItem interface locally (expand as needed for Phase 14):
```typescript
export interface NotificationItem {
  id: string
  type: string
  read: boolean
  created_at: string
  prompt?: { id: string; title: string }
}
```

Create `src/lib/api/mutations.ts` — import apiFetch, export all write functions (NO token parameters — cookie is automatic):

Functions to implement:
- `createPrompt(input: { title: string; body: string; tags: string[]; category: string; model: string; difficulty: string })` → `apiFetch('/prompts', { method: 'POST', body: JSON.stringify(input) })` → `Prompt`
- `updatePrompt(id: string, input: Partial<{ title: string; body: string; tags: string[] }>)` → `apiFetch(\`/prompts/${id}\`, { method: 'PATCH', body: JSON.stringify(input) })` → `Prompt`
- `deletePrompt(id: string)` → `apiFetch(\`/prompts/${id}\`, { method: 'DELETE' })` → `{}`
- `createVersion(promptId: string, input: { changelog: string; body: string })` → `apiFetch(\`/prompts/${promptId}/versions\`, { method: 'POST', body: JSON.stringify(input) })` → version object
- `restoreVersion(promptId: string, versionNumber: number)` → `apiFetch(\`/prompts/${promptId}/versions/${versionNumber}/restore\`, { method: 'POST' })` → version object
- `postComment(promptId: string, body: string)` → `apiFetch(\`/prompts/${promptId}/comments\`, { method: 'POST', body: JSON.stringify({ body }) })` → `CommentNode`
- `deleteComment(commentId: string)` → `apiFetch(\`/comments/${commentId}\`, { method: 'DELETE' })` → `{}`
- `addReaction(promptId: string, emoji: string)` → `apiFetch(\`/prompts/${promptId}/reactions\`, { method: 'POST', body: JSON.stringify({ emoji }) })` → `{}`
- `removeReaction(promptId: string, emoji: string)` → `apiFetch(\`/prompts/${promptId}/reactions\`, { method: 'DELETE', body: JSON.stringify({ emoji }) })` → `{}`
- `addBookmark(promptId: string)` → `apiFetch('/bookmarks', { method: 'POST', body: JSON.stringify({ prompt_id: promptId }) })` → `{}`
- `removeBookmark(promptId: string)` → `apiFetch(\`/bookmarks/${promptId}\`, { method: 'DELETE' })` → `{}`
- `markNotificationRead(id: string)` → `apiFetch(\`/notifications/${id}/read\`, { method: 'POST' })` → `{}`
- `flagPrompt(promptId: string)` → Note: the v2 API does not have a dedicated flag endpoint yet (Phase 14). Implement as a no-op that throws Error('flag endpoint not yet available') for now — useFlagPrompt will not be called until the endpoint exists.

Do NOT create `src/lib/api/admin.ts` — per CONTEXT.md locked decision, admin composable rewire is Phase 14 scope.

For `src/lib/api/http.spec.ts`, write Vitest tests using `vi.stubGlobal('fetch', vi.fn())` and `import.meta.env.VITE_API_URL = 'http://localhost:8787'` setup. Test all 6 behaviors listed in the behavior block above.

For `src/lib/api/queries.spec.ts`, mock apiFetch using `vi.mock('./http', () => ({ apiFetch: vi.fn().mockResolvedValue({}) }))`. For each exported function, call it with representative arguments and verify `apiFetch` was called with a path matching the expected endpoint (use `expect.stringContaining('/prompts')` style). Also verify query string params are included when provided.

For `src/lib/api/mutations.spec.ts`, mock apiFetch the same way. For each exported mutation function, call it and verify `apiFetch` was called with the correct path and `{ method: 'POST' | 'PATCH' | 'DELETE' }`. Verify no function has a `token` parameter by checking function `.length` or TypeScript signature — the spec serves as a regression guard.
  </action>
  <verify>
    <automated>npx vitest run src/lib/api/ --reporter=verbose 2>/dev/null | tail -20</automated>
  </verify>
  <done>http.spec.ts passes all 6 tests; queries.spec.ts passes with one test per exported GET function; mutations.spec.ts passes with one test per exported write function; no `admin.ts` exists in src/lib/api/</done>
</task>

</tasks>

<verification>
- `grep -n "id: string" src/types/index.ts` confirms Prompt.id is string
- `grep -n "nodeId" src/types/index.ts` returns no results (field removed)
- `ls src/lib/api/` shows http.ts, queries.ts, mutations.ts, http.spec.ts, queries.spec.ts, mutations.spec.ts (no admin.ts)
- `npx vitest run src/lib/api/` passes all three spec files
- `grep -n "apiFetch" src/lib/api/queries.ts` confirms all GET functions use apiFetch
- `grep -rn "token" src/lib/api/mutations.ts` returns no token parameters (only function names that contain "token" as substring allowed)
</verification>

<success_criteria>
- `src/lib/api/http.ts`, `queries.ts`, `mutations.ts` exist with all required exports
- `Prompt.id` is `string` in `src/types/index.ts`, `nodeId` field absent
- `useBookmarksStore` uses `string[]` for bookmarkedIds
- `http.spec.ts` passes 6 tests (credentials, base URL, 4xx error, 5xx fallback, 204, 200)
- `queries.spec.ts` passes — one test per exported GET function verifying apiFetch call path
- `mutations.spec.ts` passes — one test per exported write function verifying apiFetch path + method
- No `src/lib/api/admin.ts` created
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-rewire/13-01-SUMMARY.md`
</output>
