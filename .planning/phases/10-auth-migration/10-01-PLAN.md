---
phase: 10-auth-migration
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/workers/api/routes/auth.spec.ts
  - src/workers/api/middleware/auth.spec.ts
  - src/workers/api/middleware/role.spec.ts
  - src/stores/useAuthStore.spec.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07, AUTH-08, AUTH-09, AUTH-10]

must_haves:
  truths:
    - "Running npm run test:workers produces failing tests for all worker auth behaviors (RED state)"
    - "Running npm run test produces a failing test for useAuthStore poll-on-close behavior (RED state)"
    - "No test file references unimplemented stubs from outside the auth domain"
  artifacts:
    - path: "src/workers/api/routes/auth.spec.ts"
      provides: "Failing integration tests for OAuth callback, JWT signing, cookie setting, GET /me, dev-login"
      exports: []
    - path: "src/workers/api/middleware/auth.spec.ts"
      provides: "Failing unit tests for requireAuth (token_missing, token_expired) and optionalAuth"
      exports: []
    - path: "src/workers/api/middleware/role.spec.ts"
      provides: "Failing unit test for requireMaintainer 403 behavior"
      exports: []
    - path: "src/stores/useAuthStore.spec.ts"
      provides: "Updated spec replacing postMessage tests with poll-on-close and /me tests"
      exports: []
  key_links:
    - from: "src/workers/api/routes/auth.spec.ts"
      to: "src/workers/api/routes/auth.ts"
      via: "import app from '../index'"
      pattern: "import app"
    - from: "src/workers/api/middleware/auth.spec.ts"
      to: "src/workers/api/middleware/auth.ts"
      via: "import { requireAuth, optionalAuth }"
      pattern: "requireAuth"
    - from: "src/stores/useAuthStore.spec.ts"
      to: "src/stores/useAuthStore.ts"
      via: "import { useAuthStore }"
      pattern: "useAuthStore"
---

<objective>
Write all Wave 0 test scaffolds for Phase 10 auth behaviors. Every test must be in RED state (failing) so that implementation plans turn them GREEN.

Purpose: Nyquist compliance — no implementation task may begin without a corresponding failing test. All 10 AUTH requirements and DEV-04's testable behaviors must have automated test coverage set up before implementation starts.

Output: 4 spec files (3 new worker tests + 1 updated frontend store test) in RED state.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-auth-migration/10-CONTEXT.md
@.planning/phases/10-auth-migration/10-RESEARCH.md
@src/workers/api/index.spec.ts
@src/workers/api/index.ts
@src/workers/api/middleware/auth.ts
@src/workers/api/middleware/role.ts
@src/workers/api/routes/auth.ts
@src/stores/useAuthStore.spec.ts
</context>

<interfaces>
<!-- Existing test infrastructure pattern (from src/workers/api/index.spec.ts) -->
```typescript
import { describe, it, expect } from 'vitest'
import { env, createExecutionContext, waitOnExecutionContext } from 'cloudflare:test'
import app from './index'

// Pattern: fire request against the full Hono app, check Response
const req = new Request('http://localhost/health')
const ctx = createExecutionContext()
const res = await app.fetch(req, env, ctx)
await waitOnExecutionContext(ctx)
```

<!-- The env object from cloudflare:test provides bindings defined in vitest.workers.config.ts -->
<!-- env.DB is D1Database (local), env.ENV, env.JWT_SECRET available -->

<!-- Seed fixture IDs (from scripts/seed.sql) -->
<!-- dev-user:       id = '01DEVUSER000000000000000001', github_login = 'dev-user',       role = 'user' -->
<!-- dev-maintainer: id = '01DEVMAINT00000000000000001', github_login = 'dev-maintainer', role = 'maintainer' -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Worker auth route test scaffold (auth.spec.ts)</name>
  <files>src/workers/api/routes/auth.spec.ts</files>
  <behavior>
    - AUTH-09: GET /auth/login redirects to github.com/login/oauth/authorize with scope containing 'read:user' and 'user:email'
    - AUTH-01: GET /auth/callback?code=x exchanges code server-side; response body does NOT contain 'access_token'
    - AUTH-02: GET /auth/callback?code=x creates or updates user in D1 users table (UPSERT by github_id)
    - AUTH-03: After callback, the Set-Cookie header contains a JWT with alg=HS256, sub claim, role claim, exp ~7 days from now
    - AUTH-04: Set-Cookie header has HttpOnly, Secure, SameSite=Lax attributes
    - AUTH-07: GET /me with valid session cookie returns JSON with login, name, avatar_url, role fields
    - AUTH-08 (dev): POST /auth/dev-login with ENV=dev returns 200 and sets session cookie
    - AUTH-08 (prod): POST /auth/dev-login with ENV=prod returns 404
    - AUTH-08 (role): POST /auth/dev-login with { role: 'maintainer' } sets JWT role=maintainer
  </behavior>
  <action>
    Create src/workers/api/routes/auth.spec.ts. All tests must be RED (failing) because routes/auth.ts currently returns 501 for all requests.

    Key patterns from index.spec.ts to replicate:
    - Import: `import { env, createExecutionContext, waitOnExecutionContext } from 'cloudflare:test'`
    - Import: `import app from '../index'`
    - Pattern: fire `new Request(...)` against `app.fetch(req, env, ctx)`

    For AUTH-01 (GitHub code exchange): the callback handler will call out to github.com. In tests, mock `fetch` using `vi.stubGlobal('fetch', ...)` or use `vi.fn()` to intercept external HTTP calls. Provide a mock that returns `{ access_token: 'gha_fake' }` for the token endpoint and `{ id: 12345, login: 'testuser', name: 'Test User', avatar_url: 'https://avatars.example.com/1' }` for the user endpoint.

    For AUTH-03/04: after calling the callback, decode the JWT from Set-Cookie header (split on '=', take base64 middle segment, JSON.parse the decoded payload). Do NOT verify the signature in tests — just assert claims and cookie attributes.

    For AUTH-07 (GET /me): need to send a valid signed cookie. Use `hono/jwt` sign function in beforeEach to mint a test JWT using `env.JWT_SECRET`, then include it as `Cookie: pc_session=<token>` in the request.

    For AUTH-08 dev-login: `env.ENV` from the test environment — check vitest.workers.config.ts for its value. If ENV is not 'dev' in test env, the dev-login test must set ENV to 'dev' explicitly (pass a custom env object to `app.fetch`).

    Write all tests as `it.todo(...)` OR as `it('...', async () => { ... expect(true).toBe(false) })` to ensure RED state while still documenting behavior. Prefer proper test bodies with real assertions that will fail for the right reason (not just `expect(true).toBe(false)`).
  </action>
  <verify>
    <automated>npm run test:workers -- --reporter=verbose 2>&1 | grep -E "(FAIL|PASS|✓|✗|×|auth\.spec)" | head -30</automated>
  </verify>
  <done>src/workers/api/routes/auth.spec.ts exists with test cases covering AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-07, AUTH-08, AUTH-09; all tests fail (RED state); file compiles without TypeScript errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auth middleware test scaffolds (auth.spec.ts + role.spec.ts)</name>
  <files>src/workers/api/middleware/auth.spec.ts, src/workers/api/middleware/role.spec.ts</files>
  <behavior>
    auth.spec.ts — requireAuth:
    - AUTH-05a: Request with no cookie returns 401 { error: 'unauthorized', code: 'token_missing' }
    - AUTH-05b: Request with expired JWT cookie returns 401 { error: 'unauthorized', code: 'token_expired' }
    - AUTH-05c: Request with valid JWT cookie passes through (next() called); c.get('user') is set with id, role, login
    optionalAuth:
    - Request with no cookie: next() called, c.get('user') is null (no 401)
    - Request with valid cookie: next() called, c.get('user') is populated

    role.spec.ts — requireMaintainer:
    - AUTH-06a: User with role='user' receives 403 { error: 'forbidden' }
    - AUTH-06b: User with role='maintainer' passes through
    - AUTH-06c: No user in context (unauthenticated) receives 403
  </behavior>
  <action>
    These middleware tests need a Hono test app that mounts the middleware under test on a test route, then fires requests.

    Pattern for middleware testing:
    ```typescript
    import { Hono } from 'hono'
    import { env, createExecutionContext, waitOnExecutionContext } from 'cloudflare:test'
    import type { Env } from '../index'
    import { requireAuth, optionalAuth } from './auth'

    const testApp = new Hono<Env>()
    testApp.get('/protected', requireAuth(), (c) => c.json({ ok: true }))
    testApp.get('/optional', optionalAuth(), (c) => c.json({ user: c.get('user') }))
    ```

    For expired token tests: create a JWT with exp = Math.floor(Date.now()/1000) - 60 (1 minute in the past) using `hono/jwt`'s `sign` function with the test env's JWT_SECRET.

    For role tests, the requireMaintainer middleware reads `c.get('user')`. Set it up by chaining requireAuth() before requireMaintainer() in the test app, or by injecting user into context via a setup middleware.

    All tests must be RED (current middleware stubs are pass-throughs that never block).
  </action>
  <verify>
    <automated>npm run test:workers -- --reporter=verbose 2>&1 | grep -E "(FAIL|PASS|✓|✗|×|middleware)" | head -30</automated>
  </verify>
  <done>src/workers/api/middleware/auth.spec.ts and role.spec.ts exist with test cases covering AUTH-05 and AUTH-06 behaviors; all tests fail (RED); files compile without TypeScript errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update useAuthStore spec for poll-on-close pattern (AUTH-10)</name>
  <files>src/stores/useAuthStore.spec.ts</files>
  <behavior>
    AUTH-10 new behaviors (replacing postMessage tests):
    - store.token does NOT exist as a public property (or is always null/removed)
    - store.login() opens a popup to VITE_API_URL/auth/login (not VITE_CF_WORKER_URL/login)
    - store.login() uses setInterval poll-on-close: when popup.closed becomes true, fetchMe() is called
    - store.fetchMe() calls GET /me with credentials: 'include' (no Authorization header, no token param)
    - store.fetchMe() on 200 response: sets user.value and isMaintainer based on role field from /me response
    - store.fetchMe() on non-200 response: user remains null, no error thrown (graceful)
    - store.isAuthenticated is computed from user !== null (not token !== null)
    - No postMessage listener is registered anywhere (window.addEventListener('message', ...) never called)
    - INFR-07 regression: localStorage.setItem never called with any token or credential value

    Keep passing tests that still apply:
    - logout() clears user and isMaintainer
  </behavior>
  <action>
    Replace the existing useAuthStore.spec.ts with a rewritten spec that covers the new cookie-based auth flow.

    The existing spec heavily tests the v1 postMessage + token pattern. The rewrite must:
    1. Remove tests for `receiveToken`, `fetchCurrentUser(token)`, postMessage handling, and `VITE_CF_WORKER_URL` references
    2. Remove mocks for `@/lib/github/octokit`, `@/lib/github/auth`, `@/lib/github/etag` (those won't be imported)
    3. Add mock for global `fetch` to simulate `/me` responses
    4. Add tests for the new poll-on-close pattern using vi.useFakeTimers() to advance the interval

    Mock fetch for /me:
    ```typescript
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      ok: true,
      json: () => Promise.resolve({ login: 'testuser', name: 'Test', avatar_url: 'https://...', role: 'user' }),
    }))
    ```

    For popup poll-on-close test: mock window.open to return a fake popup object. Use vi.useFakeTimers(). Call store.login(). Simulate popup.closed = true. Advance timers by 200ms. Verify fetchMe was called (fetch was called with /me URL).

    All new AUTH-10 tests must be RED (useAuthStore still has token ref and postMessage pattern).
    Keep passing tests (logout behavior) as-is.

    Stub VITE_API_URL: `vi.stubEnv('VITE_API_URL', 'http://localhost:8787')`
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose 2>&1 | grep -E "(FAIL|PASS|useAuthStore)" | head -20</automated>
  </verify>
  <done>src/stores/useAuthStore.spec.ts updated: v1 postMessage/token tests replaced with poll-on-close and /me tests; new AUTH-10 tests are RED; any remaining passing tests (logout) still pass; file compiles</done>
</task>

</tasks>

<verification>
After all tasks:
- `npm run test:workers` — shows new spec files, all auth tests failing (RED)
- `npm run test` — useAuthStore spec shows new tests failing (RED)
- `tsc --noEmit` on worker files — no TypeScript compilation errors in spec files themselves
- No test files reference unimplemented behaviors outside the auth domain
</verification>

<success_criteria>
- 3 new worker spec files created: auth.spec.ts (routes), auth.spec.ts (middleware), role.spec.ts (middleware)
- 1 existing spec file updated: src/stores/useAuthStore.spec.ts
- All new tests fail for the RIGHT reason (behavior not implemented, not import/compile errors)
- Test names precisely map to AUTH-01 through AUTH-10 requirement behaviors
- `npm run test:workers` exits non-zero (tests red) but runs without crash
- `npm run test` exits non-zero (AUTH-10 tests red) but existing non-auth tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-auth-migration/10-01-SUMMARY.md`
</output>
