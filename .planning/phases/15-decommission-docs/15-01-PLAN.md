---
phase: 15-decommission-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/github/octokit.ts
  - src/lib/github/octokit.spec.ts
  - src/lib/github/queries.ts
  - src/lib/github/mutations.ts
  - src/lib/github/etag.ts
  - src/lib/github/etag.spec.ts
  - src/lib/github/auth.ts
  - src/lib/github/auth.spec.ts
  - wrangler.toml
  - src/workers/oauth.ts
  - src/workers/oauth.spec.ts
  - package.json
  - .env.example
  - src/router/index.ts
autonomous: true
requirements:
  - DECOM-01
  - DECOM-02
  - DECOM-03
  - DECOM-04
  - DECOM-05
  - DECOM-07
  - DECOM-08

must_haves:
  truths:
    - "Running `npm run build` succeeds with no type errors after all github lib files are deleted"
    - "Running `npm test && npm run test:workers` passes with no failures after deletions"
    - "No file in src/ imports from `@/lib/github` or `src/lib/github`"
    - "`deploy:worker` script in package.json uses `--config src/workers/api/wrangler.toml`"
    - "`.env.example` contains no v1 variables (VITE_GITHUB_OWNER, VITE_GITHUB_REPO, VITE_GITHUB_CLIENT_ID, VITE_GITHUB_APP_INSTALLATION_ID)"
    - "`@octokit/core` and `@octokit/graphql` are removed from package.json dependencies"
  artifacts:
    - path: "src/lib/github/"
      provides: "Deleted — directory must not exist after this plan"
    - path: "package.json"
      provides: "Updated deploy:worker script, removed @octokit/* deps"
    - path: ".env.example"
      provides: "v2-only env vars — no v1 GitHub vars"
    - path: "src/router/index.ts"
      provides: "Stale Phase 10 comment removed"
  key_links:
    - from: "src/ (all files)"
      to: "src/lib/github/"
      via: "import"
      pattern: "from.*@/lib/github|from.*src/lib/github"
---

<objective>
Delete all v1 GitHub-Issues code — the entire `src/lib/github/` directory, the root `wrangler.toml` (v1 OAuth proxy config), `src/workers/oauth.ts` and its spec — then clean up residual references: fix the `deploy:worker` npm script, remove dead octokit packages, clean `.env.example` of v1 vars, and remove the stale router comment.

Purpose: Remove the last v1 code paths so the codebase only contains v2 Cloudflare Workers + D1 architecture. The build gate (`npm run build`) and test suites confirm nothing living was accidentally severed.

Output: `src/lib/github/` directory deleted (8 files), root `wrangler.toml` deleted, `oauth.ts` + `oauth.spec.ts` deleted, `package.json` updated, `.env.example` rewritten, `src/router/index.ts` comment removed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-decommission-docs/15-RESEARCH.md

<interfaces>
<!-- Key facts about files being deleted and cleaned up. No exploration needed. -->

Files to DELETE completely (with rm or equivalent):
  src/lib/github/octokit.ts        (DECOM-01)
  src/lib/github/octokit.spec.ts   (companion spec)
  src/lib/github/queries.ts        (DECOM-02)
  src/lib/github/mutations.ts      (DECOM-03)
  src/lib/github/etag.ts           (DECOM-04)
  src/lib/github/etag.spec.ts      (companion spec)
  src/lib/github/auth.ts           (DECOM-05)
  src/lib/github/auth.spec.ts      (companion spec)
  wrangler.toml                    (DECOM-07 — root-level, v1 OAuth worker)
  src/workers/oauth.ts             (v1 OAuth proxy, implied by DECOM-07)
  src/workers/oauth.spec.ts        (companion spec)

Safe to delete confirmed by grep analysis (RESEARCH.md):
  - No source file outside src/lib/github/ imports from it
  - Only references found: router/index.ts line 4-7 (comment only), two spec assertion strings (not imports)

deploy:worker fix (current → correct):
  Current:  "deploy:worker": "wrangler deploy"
  Fixed:    "deploy:worker": "wrangler deploy --config src/workers/api/wrangler.toml"

octokit packages to remove from package.json dependencies:
  "@octokit/core": "^7.0.6"
  "@octokit/graphql": "^9.0.3"

.env.example v1 vars to REMOVE:
  VITE_GITHUB_OWNER
  VITE_GITHUB_REPO
  VITE_GITHUB_CLIENT_ID
  VITE_GITHUB_APP_INSTALLATION_ID

.env.example vars to KEEP:
  VITE_API_URL       (v2 var)
  VITE_CF_WORKER_URL (image upload R2 proxy — not in scope for decommission per RESEARCH)
  VITE_R2_PUBLIC_URL (image storage — keep)

Router stale comment (src/router/index.ts lines 4-7) — DELETE these lines:
  // NOTE: verifyMaintainerStatus re-export removed in Phase 10 (cookie-based auth).
  // The router now uses authStore.isMaintainer (derived from user.role in JWT cookie).
  // Any callers that imported verifyMaintainerStatus from here should import directly
  // from @/lib/github/auth, or update to Phase 13 approach (to be cleaned up in Phase 15).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all v1 GitHub lib files and root wrangler.toml</name>
  <files>
    src/lib/github/octokit.ts, src/lib/github/octokit.spec.ts,
    src/lib/github/queries.ts, src/lib/github/mutations.ts,
    src/lib/github/etag.ts, src/lib/github/etag.spec.ts,
    src/lib/github/auth.ts, src/lib/github/auth.spec.ts,
    wrangler.toml, src/workers/oauth.ts, src/workers/oauth.spec.ts
  </files>
  <action>
    Delete the entire `src/lib/github/` directory and all 11 listed files using `rm -rf src/lib/github/` then individually remove `wrangler.toml`, `src/workers/oauth.ts`, and `src/workers/oauth.spec.ts`.

    After deletion, remove the stale comment from `src/router/index.ts` — the comment block is lines 4–7 (the four lines starting with "// NOTE: verifyMaintainerStatus re-export removed..."). The file should retain only the live imports and router setup code.

    Do NOT touch `src/workers/upload.ts` or `src/workers/upload.spec.ts` — image upload is out of scope per RESEARCH.md (no DECOM requirement for it).

    Verify no import from `@/lib/github` survives: `grep -r "from.*@/lib/github\|from.*lib/github" src/ --include="*.ts" --include="*.vue"` should return empty (test assertion strings in spec files are fine — they are string literals, not imports).
  </action>
  <verify>
    <automated>grep -r "from.*@/lib/github\|from.*lib/github" /Users/davor/development/repo/d-sauer/prompt-community/src/ --include="*.ts" --include="*.vue" && echo "FOUND IMPORTS - FAIL" || echo "No live imports - OK"</automated>
  </verify>
  <done>
    All 8 `src/lib/github/` files deleted, directory removed. Root `wrangler.toml` deleted. `oauth.ts` and `oauth.spec.ts` deleted. Router stale comment removed. Zero live imports from `@/lib/github` remain in src/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean package.json, .env.example, and verify full build + test pass</name>
  <files>package.json, .env.example</files>
  <action>
    1. **Fix `deploy:worker` script in `package.json`:**
       Change: `"deploy:worker": "wrangler deploy"`
       To:     `"deploy:worker": "wrangler deploy --config src/workers/api/wrangler.toml"`

    2. **Remove dead octokit packages from `package.json` dependencies:**
       Run: `npm uninstall @octokit/core @octokit/graphql`
       This removes both entries from `dependencies` in package.json and updates package-lock.json.

    3. **Rewrite `.env.example`** to contain only v2 variables. Replace the entire file with:
       ```
       # .env.example — copy to .env.local and fill in values
       # Do NOT commit .env.local to version control

       # v2 API base URL (Hono worker on localhost in dev; deployed CF worker URL in prod)
       VITE_API_URL=http://localhost:8787

       # Cloudflare Worker for image uploads (R2 proxy — separate from v2 API)
       VITE_CF_WORKER_URL=https://upload.your-domain.workers.dev

       # Cloudflare R2 public URL for image storage
       VITE_R2_PUBLIC_URL=https://assets.your-domain.com
       ```

    4. **Run full build + test gate** to confirm nothing was accidentally severed:
       `npm run build && npm test && npm run test:workers`

    All three must pass cleanly. If any test fails, the deletion in Task 1 accidentally cut a live dependency — investigate the failing test import and fix.
  </action>
  <verify>
    <automated>cd /Users/davor/development/repo/d-sauer/prompt-community && npm run build && npm test && npm run test:workers</automated>
  </verify>
  <done>
    `deploy:worker` script points to API wrangler.toml. `@octokit/core` and `@octokit/graphql` absent from package.json. `.env.example` contains only `VITE_API_URL`, `VITE_CF_WORKER_URL`, `VITE_R2_PUBLIC_URL` — no v1 GitHub vars. `npm run build`, `npm test`, and `npm run test:workers` all pass with exit 0.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -r "VITE_GITHUB_OWNER\|VITE_GITHUB_REPO\|VITE_GITHUB_CLIENT_ID\|VITE_GITHUB_APP_INSTALLATION_ID" .env.example` returns empty
- `grep "@octokit" package.json` returns empty
- `cat package.json | python3 -c "import sys,json; s=json.load(sys.stdin)['scripts']; print(s['deploy:worker'])"` prints `wrangler deploy --config src/workers/api/wrangler.toml`
- `ls src/lib/github/ 2>&1` returns "No such file or directory"
- `npm run build && npm test && npm run test:workers` all exit 0
</verification>

<success_criteria>
The v1 GitHub-Issues code paths are completely removed from the codebase. The codebase compiles, all tests pass, and the `deploy:worker` script correctly targets the v2 API worker. A developer reading `package.json` and `.env.example` sees only v2 configuration — no trace of `@octokit`, `VITE_GITHUB_OWNER`, or the old OAuth proxy worker.
</success_criteria>

<output>
After completion, create `.planning/phases/15-decommission-docs/15-01-SUMMARY.md`
</output>
