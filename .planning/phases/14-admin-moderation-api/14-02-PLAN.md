---
phase: 14-admin-moderation-api
plan: "02"
type: execute
wave: 1
depends_on:
  - "14-01"
files_modified:
  - src/workers/api/routes/admin.ts
  - src/workers/api/routes/labels.ts
  - src/workers/api/middleware/role.ts
autonomous: true
requirements:
  - API-22
  - API-23
  - API-24
  - API-25
  - API-26
  - API-27
  - API-28

must_haves:
  truths:
    - "GET /admin/queue returns flagged prompts only, paginated, maintainer-only (401 without JWT, 403 for user role)"
    - "GET /admin/log returns moderation_log history, maintainer-only"
    - "POST /admin/prompts/:id/approve updates prompt status to 'published' and inserts a moderation_log row"
    - "POST /admin/prompts/:id/hide updates prompt status to 'hidden' and inserts a moderation_log row"
    - "POST/PATCH/DELETE /labels succeed for maintainer, return 403 for non-maintainer"
    - "GET /admin/stats returns { total, flagged } counts from D1"
    - "All error responses across admin.ts, labels.ts, and middleware have { error: string, code: string } shape (requireMaintainer fixed)"
  artifacts:
    - path: "src/workers/api/routes/admin.ts"
      provides: "Six admin route handlers replacing the 501 stub"
      exports: ["default app"]
    - path: "src/workers/api/routes/labels.ts"
      provides: "POST, PATCH, DELETE handlers added to existing GET"
      exports: ["default app"]
    - path: "src/workers/api/middleware/role.ts"
      provides: "requireMaintainer returns { error: 'forbidden', code: 'forbidden' } — API-27 fix"
      exports: ["requireMaintainer"]
  key_links:
    - from: "src/workers/api/routes/admin.ts"
      to: "src/workers/api/middleware/auth.ts"
      via: "requireAuth(), requireMaintainer() chained in that order"
      pattern: "requireAuth\\(\\).*requireMaintainer\\(\\)"
    - from: "src/workers/api/routes/admin.ts"
      to: "src/workers/api/db/schema.ts"
      via: "moderation_log insert after every approve/hide"
      pattern: "moderation_log"
---

<objective>
Implement all admin route handlers and label write handlers, turning the RED test suite from Plan 01 GREEN.

Purpose: Fills the 501 stub in admin.ts with six handler functions, extends labels.ts with write endpoints, and fixes the requireMaintainer middleware to include the `code` field required by API-27.
Output: Working admin API — queue, log, approve, hide, label CRUD, stats — all behind requireAuth + requireMaintainer middleware.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-admin-moderation-api/14-RESEARCH.md
@.planning/phases/14-admin-moderation-api/14-01-SUMMARY.md

@src/workers/api/routes/admin.ts
@src/workers/api/routes/labels.ts
@src/workers/api/middleware/auth.ts
@src/workers/api/middleware/role.ts
@src/workers/api/db/schema.ts
@src/workers/api/routes/prompts.ts
</context>

<interfaces>
<!-- requireAuth already returns { error, code } — confirmed in auth.ts -->
<!-- requireMaintainer currently returns { error: 'forbidden' } — MISSING code field — fix in Task 1 -->
```typescript
// src/workers/api/middleware/role.ts — current (broken API-27):
return c.json({ error: 'forbidden' }, 403)
// Fix to:
return c.json({ error: 'forbidden', code: 'forbidden' }, 403)
```

<!-- Drizzle + D1 handler pattern from prompts.ts -->
```typescript
import { drizzle } from 'drizzle-orm/d1'
import { eq, desc, sql } from 'drizzle-orm'
import { ulid } from 'ulid'
import * as schema from '../db/schema'

const db = drizzle(c.env.DB, { schema })
const tsNow = sql`(strftime('%Y-%m-%dT%H:%M:%fZ', 'now'))`
```

<!-- moderation_log schema (from schema.ts) -->
```typescript
moderation_log: {
  id: text, prompt_id: text (FK no cascade), actor_id: text (FK no cascade),
  action: text ('approve' | 'hide'), reason: text | null,
  created_at: text (ISO 8601, tsNow default)
}
```

<!-- Label schema -->
```typescript
labels: { id: text, prefix: text, value: text, color: text | null, description: text | null }
```

<!-- Middleware composition order (CRITICAL) -->
```typescript
app.get('/queue', requireAuth(), requireMaintainer(), async (c) => { ... })
// requireAuth MUST come before requireMaintainer — see RESEARCH Pattern 1
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix requireMaintainer error shape + implement admin.ts handlers</name>
  <files>src/workers/api/middleware/role.ts, src/workers/api/routes/admin.ts</files>
  <action>
    **Step 1 — Fix role.ts (API-27):**
    Edit `src/workers/api/middleware/role.ts`. Find the line returning `{ error: 'forbidden' }` and change to:
    ```typescript
    return c.json({ error: 'forbidden', code: 'forbidden' }, 403)
    ```
    This is the ONLY change to role.ts.

    **Step 2 — Implement admin.ts:**
    Replace the entire content of `src/workers/api/routes/admin.ts` with a Hono sub-app that registers these routes (all paths relative to the `/admin` mount point in index.ts):

    Imports needed: hono, drizzle-orm/d1, drizzle-orm (eq, desc, sql, count), ulid, requireAuth from middleware/auth, requireMaintainer from middleware/role, schema from db/schema, Env type from index.

    Routes to implement:

    **GET /queue** (API-22, API-28):
    - Chain: requireAuth(), requireMaintainer()
    - Query: SELECT from prompts WHERE status='flagged', orderBy desc(created_at), limit 20+1 for cursor pagination
    - Batch-fetch author login + avatar_url for each result (inArray on users.id) — same pattern as prompts.ts GET /
    - Cursor: if results > 20, slice to 20, set next_cursor = last item id; else next_cursor = null
    - Response: `c.json({ data: items, next_cursor })`

    **GET /log** (API-23, API-28):
    - Chain: requireAuth(), requireMaintainer()
    - Query: SELECT from moderation_log, LEFT JOIN prompts (id, title), LEFT JOIN users as actor (id, login), orderBy desc(created_at), LIMIT 500
    - Response: `c.json({ data: entries })` — entries shape: { id, action, reason, prompt: { id, title }, actor: { login }, created_at }
    - Use Drizzle leftJoin pattern. If Drizzle join syntax is complex, use raw D1 prepare with SQL: `SELECT ml.*, p.title as prompt_title, u.login as actor_login FROM moderation_log ml LEFT JOIN prompts p ON p.id = ml.prompt_id LEFT JOIN users u ON u.id = ml.actor_id ORDER BY ml.created_at DESC LIMIT 500`

    **POST /prompts/:id/approve** (API-24, API-28):
    - Chain: requireAuth(), requireMaintainer()
    - Read prompt by id — 404 if not found
    - Parse optional body `{ reason?: string }`; body parse failure is ok (reason defaults null)
    - UPDATE prompts SET status='published', updated_at=tsNow WHERE id=:id
    - INSERT INTO moderation_log { id: ulid(), prompt_id, actor_id: c.var.user.id, action: 'approve', reason: body?.reason ?? null }
    - Response: `c.json({ success: true })` 200

    **POST /prompts/:id/hide** (API-25, API-28):
    - Same pattern as approve but status='hidden' and action='hide'

    **GET /stats** (supports FRONT-06/useAdminStats):
    - Chain: requireAuth(), requireMaintainer()
    - Two COUNT queries: total published prompts, total flagged prompts
    - Use `db.select({ count: count() }).from(schema.prompts).where(eq(schema.prompts.status, 'published'))` pattern (import count from drizzle-orm)
    - Response: `c.json({ total: totalCount, flagged: flaggedCount })`

    Route order: register /queue, /log, /stats BEFORE /prompts/:id/approve and /prompts/:id/hide to avoid Hono path conflicts.

    Do NOT set Cache-Control on any admin route (no caching for admin data).
  </action>
  <verify>
    <automated>npx vitest run --config vitest.workers.config.ts src/workers/api/routes/admin.spec.ts 2>&1 | grep -E "passed|failed|error" | tail -5</automated>
  </verify>
  <done>Admin queue, log, approve, hide, stats endpoints return correct responses; requireMaintainer now returns { error, code }; admin.spec.ts queue/log/approve/hide/stats tests pass GREEN</done>
</task>

<task type="auto">
  <name>Task 2: Label write handlers + full API-27 error shape audit</name>
  <files>src/workers/api/routes/labels.ts</files>
  <action>
    **Step 1 — Add write handlers to labels.ts:**
    Append three route handlers to `src/workers/api/routes/labels.ts` after the existing GET / handler. Add missing imports: requireAuth, requireMaintainer from middleware, eq from drizzle-orm, ulid from ulid.

    **POST /** (API-26, API-28):
    - Chain: requireAuth(), requireMaintainer()
    - Parse body `{ prefix: string, value: string, color?: string, description?: string }`
    - Validate: prefix and value must be non-empty strings; if not, return `c.json({ error: 'validation_error', code: 'validation_error' }, 422)`
    - Insert: `const id = ulid(); await db.insert(schema.labels).values({ id, prefix, value, color: body.color ?? null, description: body.description ?? null })`
    - Re-fetch and return: `const [row] = await db.select().from(schema.labels).where(eq(schema.labels.id, id)).limit(1); return c.json(row, 201)`

    **PATCH /:id** (API-26, API-28):
    - Chain: requireAuth(), requireMaintainer()
    - Find label by id — `const [existing] = await db.select()...where(eq(schema.labels.id, id)).limit(1)`
    - 404 if not found: `c.json({ error: 'not_found', code: 'not_found' }, 404)`
    - Parse body — accept any subset of { prefix, value, color, description }
    - Build update object with only present fields (skip undefined)
    - UPDATE: `await db.update(schema.labels).set(updates).where(eq(schema.labels.id, id))`
    - Re-fetch and return 200

    **DELETE /:id** (API-26, API-28):
    - Chain: requireAuth(), requireMaintainer()
    - 404 if label not found
    - DELETE: `await db.delete(schema.labels).where(eq(schema.labels.id, id))`
    - Return 204 with empty body: `return new Response(null, { status: 204 })`

    Do NOT add Cache-Control header on write routes (GET already sets it, write handlers must not).

    **Step 2 — API-27 error shape audit (no file changes if all clean):**
    Run this grep to identify any error responses missing the `code` field:
    ```
    grep -n "c\.json.*error" src/workers/api/routes/*.ts src/workers/api/middleware/*.ts
    ```
    For each hit: verify the object contains BOTH `error` and `code`. Fix any that only have `error` (role.ts was fixed in Task 1; check all others). Document findings in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.workers.config.ts src/workers/api/routes/admin.spec.ts 2>&1 | grep -E "passed|failed" | tail -5</automated>
  </verify>
  <done>POST/PATCH/DELETE /labels tests GREEN in admin.spec.ts; API-27 audit complete with all gaps fixed; full admin.spec.ts test suite passes GREEN</done>
</task>

</tasks>

<verification>
After both tasks:
```
npx vitest run --config vitest.workers.config.ts src/workers/api/routes/admin.spec.ts
```
Expected: all tests GREEN.

Full suite sanity check:
```
npx vitest run && npx vitest run --config vitest.workers.config.ts
```
Expected: no regressions from role.ts fix.
</verification>

<success_criteria>
- `admin.ts` stub replaced with six working route handlers (queue, log, approve, hide, stats — plus sub-paths)
- `labels.ts` extended with POST, PATCH, DELETE handlers, all guarded by requireAuth + requireMaintainer
- `role.ts` requireMaintainer returns `{ error: 'forbidden', code: 'forbidden' }`
- `admin.spec.ts` from Plan 01 fully GREEN
- API-27 audit grep produces zero hits missing `code` field
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-moderation-api/14-02-SUMMARY.md`
</output>
