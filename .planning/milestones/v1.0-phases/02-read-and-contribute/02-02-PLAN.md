---
phase: 02-read-and-contribute
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/stores/useDraftStore.ts
  - src/stores/useDraftStore.spec.ts
  - src/workers/upload.ts
  - src/workers/upload.spec.ts
  - src/lib/github/mutations.ts
  - src/composables/queries/useCreatePrompt.ts
  - src/composables/queries/useUpdatePrompt.ts
  - src/views/PromptEditorView.vue
  - src/components/editor/EditorLayout.vue
  - src/components/editor/MarkdownEditor.vue
  - src/components/editor/MetadataFields.vue
  - src/components/editor/ImageUploadZone.vue
  - src/components/editor/PreviewPane.vue
  - src/components/editor/TagsInput.vue
  - src/components/editor/StatusBadge.vue
  - src/router/index.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - CONT-07
  - CONT-08
  - CONT-09
  - CONT-10
  - CONT-11
  - VERS-01

user_setup:
  - service: cloudflare-r2
    why: "Image uploads (CONT-05) go to Cloudflare R2 storage via the CF Worker"
    env_vars:
      - name: VITE_R2_PUBLIC_URL
        source: "Cloudflare Dashboard -> R2 -> your bucket -> Settings -> Public Access -> copy domain"
    dashboard_config:
      - task: "Create R2 bucket and enable public access"
        location: "Cloudflare Dashboard -> R2 -> Create bucket. Enable public access under Settings."
      - task: "Bind R2 bucket to Worker"
        location: "wrangler.toml: add [[r2_buckets]] binding 'R2_BUCKET' pointing to your bucket name"
      - task: "Set R2_PUBLIC_URL environment variable in Worker"
        location: "wrangler.toml: add R2_PUBLIC_URL = 'https://your-bucket.r2.dev' under [vars]"

must_haves:
  truths:
    - "Authenticated user can open the editor, fill in title + markdown body + category + model + difficulty + tags, and publish — which creates a real GitHub Issue in the data repo"
    - "Editor shows live split-pane preview of markdown as the user types (debounced 300ms)"
    - "Authenticated user can upload an image by drag-and-drop or file picker and the markdown link is inserted into the editor automatically"
    - "Editor auto-saves draft content to localStorage every 30 seconds; on page reload the draft is recovered"
    - "When navigating away from the editor with unsaved changes, a confirmation dialog appears and blocks navigation unless confirmed"
    - "Authenticated user can edit their own existing prompt by navigating to /prompts/:id/edit — the editor pre-fills with current content"
    - "Authenticated user can fork any prompt by navigating to /prompts/new?fork=:id — the editor pre-fills with forked content"
    - "Authenticated user can publish an updated version of their own prompt — creates a version comment on the GitHub Issue"
    - "Unauthenticated user navigating to /prompts/new or /prompts/:id/edit is redirected to the browse view"
    - "Content type (prompt / skill file / skill set) is selectable and stored as a label on the GitHub Issue"
  artifacts:
    - path: "src/stores/useDraftStore.ts"
      provides: "title, content, metadata, tags, isDirty, lastSaved, save(), recover(), clear() — auto-save every 30s"
      exports: ["useDraftStore"]
    - path: "src/workers/upload.ts"
      provides: "CF Worker handleUpload() — validates auth, file type/size, uploads to R2, returns URL"
      exports: ["handleUpload"]
    - path: "src/lib/github/mutations.ts"
      provides: "createIssue(), updateIssue(), createVersionComment() REST helpers"
      exports: ["createIssue", "updateIssue", "createVersionComment"]
    - path: "src/composables/queries/useCreatePrompt.ts"
      provides: "TanStack mutation — POST /issues via createIssue; invalidates prompts query on success"
      exports: ["useCreatePrompt"]
    - path: "src/composables/queries/useUpdatePrompt.ts"
      provides: "TanStack mutation — PATCH /issues/:number; also createVersionComment for VERS-01"
      exports: ["useUpdatePrompt"]
    - path: "src/views/PromptEditorView.vue"
      provides: "Full editor view wiring all editor components; handles create/edit/fork/version modes"
  key_links:
    - from: "src/views/PromptEditorView.vue"
      to: "src/stores/useDraftStore.ts"
      via: "mounts draftStore.recover() on created; calls draftStore.save() on publish success; router guard reads draftStore.isDirty"
      pattern: "useDraftStore"
    - from: "src/stores/useDraftStore.ts"
      to: "VueUse useLocalStorage + useInterval"
      via: "useLocalStorage('draft:new') for persistence; useInterval(save, 30_000) for auto-save"
      pattern: "useInterval|useLocalStorage"
    - from: "src/components/editor/ImageUploadZone.vue"
      to: "src/workers/upload.ts"
      via: "POST multipart/form-data to VITE_CF_WORKER_URL + '/api/upload' with Authorization: token"
      pattern: "api/upload|Authorization"
    - from: "src/router/index.ts"
      to: "src/stores/useDraftStore.ts"
      via: "beforeEach guard checks draftStore.isDirty; shows confirm dialog via useUIStore"
      pattern: "isDirty|draftStore"
---

<objective>
Build the content editor — the split-pane markdown editor with live preview, metadata fields, image upload, auto-save, unsaved-changes guard, create/edit/fork/version modes, and publish to GitHub Issues.

Purpose: Authenticated users create and update content. This is the contribution side of the read-and-contribute core loop. Depends on 02-01 for usePromptDetail (edit/fork pre-fill) and src/lib/frontmatter.ts (YAML construction).

Output: Fully functional PromptEditorView with all editor components, useDraftStore with auto-save, CF Worker image upload endpoint, and GitHub REST mutations for create/update/version.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-read-and-contribute/02-RESEARCH.md
@.planning/phases/02-read-and-contribute/02-VALIDATION.md
@.planning/phases/02-read-and-contribute/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from 02-01 this plan depends on. -->

From src/lib/frontmatter.ts (created in 02-01):
```typescript
export interface PromptFrontmatter {
  type: 'prompt' | 'skill-file' | 'skill-set'
  category: string
  model: string
  difficulty: 'beginner' | 'intermediate' | 'advanced'
  tags: string[]
  version: number
  changelog?: string
}

export function buildFrontmatter(meta: PromptFrontmatter): string  // returns '---\n[YAML]\n---\n\n'
export function parseFrontmatter(body: string): { data: Partial<PromptFrontmatter>; content: string }
```

From src/lib/markdown.ts (created in 02-01):
```typescript
export async function initMarkdown(): Promise<MarkdownIt>  // singleton, already initialized by App.vue
export function renderMarkdown(md: MarkdownIt, content: string): string
// Access singleton: import { initMarkdown } from '@/lib/markdown' and call with no args to get the cached instance
```

From src/composables/queries/usePromptDetail.ts (created in 02-01):
```typescript
export function usePromptDetail(id: Ref<number | null> | number | null): {
  data: Ref<Prompt | undefined>
  isLoading: Ref<boolean>
  // standard TanStack Query return
}
```

From src/lib/github/octokit.ts (Phase 1):
```typescript
export function createRestClient(token: string): Octokit
// Use for authenticated REST mutations: POST /issues, PATCH /issues/:number, POST /issues/:number/comments
```

From src/stores/useAuthStore.ts (Phase 1):
```typescript
// authStore.token: string | null
// authStore.isAuthenticated: boolean
// Guard: if !authStore.isAuthenticated, redirect to /browse
```

From src/types/index.ts (extended in 02-01):
```typescript
export interface Prompt { id: number; title: string; body: string; frontmatter: PromptFrontmatter; ... }
export type ContentType = 'prompt' | 'skill-file' | 'skill-set'
```

Router routes (from Phase 1, confirm in router/index.ts):
- /prompts/new → PromptEditorView (create mode)
- /prompts/:id/edit → PromptEditorView (edit mode, requires auth)
- /prompts/new?fork=:id → PromptEditorView (fork mode via query param)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test stubs + useDraftStore + CF Worker upload</name>
  <files>
    src/stores/useDraftStore.spec.ts,
    src/workers/upload.spec.ts,
    src/stores/useDraftStore.ts,
    src/workers/upload.ts
  </files>
  <behavior>
    Wave 0 test stubs (it.todo — RED):
    useDraftStore.spec.ts: "auto-save fires after 30s interval (fake timers)", "isDirty becomes true when title changes", "isDirty becomes false after save()", "recover() restores from localStorage", "clear() empties draft and resets isDirty"
    upload.spec.ts: "rejects request without Authorization header (401)", "rejects unsupported file type (400)", "rejects file over 10MB (413)", "accepts valid PNG and returns R2 URL"

    useDraftStore implementation:
    - Pinia setup store using VueUse useLocalStorage for persistence and useInterval for auto-save
    - draftKey ref defaults to 'draft:new'; recover(promptId?) sets draftKey to 'draft:{id}' or 'draft:new'
    - watch([title, content, metadata, tags], () => { isDirty.value = true }, { deep: true })
    - useInterval(save, 30_000) for auto-save every 30 seconds
    - save() writes to persistedDraft, sets lastSaved = new Date(), sets isDirty = false
    - clear() resets persistedDraft = {}, isDirty = false, lastSaved = null
    - DraftState interface: { title: string; content: string; metadata: Partial<PromptFrontmatter>; tags: string[] }

    upload.ts CF Worker:
    - Export handleUpload(request: Request, env: Env): Promise<Response>
    - Check Authorization header starts with 'token ' — return 401 if missing
    - Parse formData, get 'file' field — return 400 if missing
    - Validate file.type in ['image/png','image/jpeg','image/gif','image/webp'] — return 400 if not
    - Validate file.size <= 10 * 1024 * 1024 — return 413 if exceeded
    - key = `uploads/${crypto.randomUUID()}-${file.name}`
    - await env.R2_BUCKET.put(key, file.stream(), { httpMetadata: { contentType: file.type } })
    - Return Response.json({ url: `${env.R2_PUBLIC_URL}/${key}` })
    - Handle OPTIONS preflight: return 204 with Access-Control-Allow-Origin: env.APP_ORIGIN, Access-Control-Allow-Methods: 'POST, OPTIONS', Access-Control-Allow-Headers: 'Authorization, Content-Type'
    - Add CORS header Access-Control-Allow-Origin: env.APP_ORIGIN to all responses (avoids Pitfall 8)
    - Env interface: { R2_BUCKET: R2Bucket; R2_PUBLIC_URL: string; APP_ORIGIN: string }
  </behavior>
  <action>
    Write Wave 0 stubs first so Vitest sees them as todo (not failures). Then implement stores and worker.

    useDraftStore note: useLocalStorage from VueUse returns a reactive ref that syncs automatically — no manual JSON.parse/stringify needed. The persistedDraft is typed as `Ref<Partial<DraftState>>` using useLocalStorage<Partial<DraftState>>(draftKey, {}).

    upload.ts note: This is a new Worker route to add alongside the existing OAuth Worker. Integrate it into the main Worker export in wrangler.toml or add as a separate route. The simplest approach: add a `/api/upload` route handler in the existing oauth.ts Worker by checking `url.pathname === '/api/upload'` and delegating to handleUpload. This avoids a second Worker deployment.

    For tests: upload.spec.ts uses vi.fn() to mock R2Bucket.put and tests the validation logic. The R2 binding is passed as part of the env mock object.
  </action>
  <verify>
    <automated>npx vitest run src/stores/useDraftStore.spec.ts src/workers/upload.spec.ts</automated>
  </verify>
  <done>Both spec files pass (todo stubs recognized). useDraftStore exports title, content, metadata, tags, isDirty, lastSaved, save, recover, clear. upload.ts exports handleUpload. TypeScript compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GitHub REST mutations (mutations.ts, useCreatePrompt, useUpdatePrompt)</name>
  <files>
    src/lib/github/mutations.ts,
    src/composables/queries/useCreatePrompt.ts,
    src/composables/queries/useUpdatePrompt.ts
  </files>
  <behavior>
    mutations.ts:
    - createIssue(token, title, body, labelNames): calls octokit.request('POST /repos/{owner}/{repo}/issues', { title, body, labels: labelNames, headers: {'X-GitHub-Api-Version':'2022-11-28'} }). Returns created issue number.
    - updateIssue(token, issueNumber, title, body, labelNames): calls octokit.request('PATCH /repos/{owner}/{repo}/issues/{issue_number}', {...}). Returns updated issue.
    - createVersionComment(token, issueNumber, versionNumber, changelog, content): posts comment with body `## Version ${versionNumber} — ${date}\n${changelog}\n\n${content}`. Returns comment id.
    - VITE_GITHUB_OWNER and VITE_GITHUB_REPO read from import.meta.env.

    useCreatePrompt:
    - TanStack useMutation; mutationFn calls buildFrontmatter(meta) + createIssue(token, title, body, labels)
    - Labels derived from metadata: ['category:'+category, 'model:'+model, 'difficulty:'+difficulty, 'type:'+type]
    - onSuccess: queryClient.resetQueries(['prompts']) to refetch from page 1 (avoids Pitfall 5 duplicates); also add new prompt to searchStore via buildIndex([...allPrompts, newPrompt])
    - onSuccess: call draftStore.clear() to discard saved draft; router.push('/prompts/:newId')

    useUpdatePrompt:
    - TanStack useMutation; two modes: edit (PATCH issue) and version (POST comment)
    - For edit mode: updateIssue(token, issueNumber, title, newBody, labels)
    - For version publish (VERS-01): createVersionComment(token, issueNumber, nextVersionNumber, changelog, content); then updateIssue to bump version number in frontmatter
    - onSuccess: queryClient.invalidateQueries(['prompt', issueNumber]) to refresh detail cache
  </behavior>
  <action>
    Create src/lib/github/mutations.ts with three helper functions. Create two composable query files.

    Label construction: Build label array from metadata fields. Labels follow namespace:value pattern (category:Engineering, model:claude-3-5-sonnet, difficulty:intermediate, type:prompt). These labels must exist in the GitHub data repo — document in task output that the user must create the GitHub labels in the data repo before prompts will filter correctly.

    useCreatePrompt: Use `useAuthStore().token` for the octokit client. Guard against null token (mutation should not be called when unauthenticated — the route guard handles this, but add a runtime guard in mutationFn as defense).

    useUpdatePrompt for VERS-01: The new version number is max(existing versions) + 1. This requires having the version history from usePromptVersions — but that composable is in Plan 02-03. For now, useUpdatePrompt accepts `versionNumber` as an explicit param. The editor UI (PromptEditorView) will compute it from the prompt's current `frontmatter.version` + 1 when calling useUpdatePrompt.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ src/composables/ 2>&1 | tail -20</automated>
  </verify>
  <done>mutations.ts exports createIssue, updateIssue, createVersionComment. useCreatePrompt and useUpdatePrompt export mutation composables. TypeScript compiles with zero errors.</done>
</task>

<task type="auto">
  <name>Task 3: PromptEditorView and editor components</name>
  <files>
    src/views/PromptEditorView.vue,
    src/components/editor/EditorLayout.vue,
    src/components/editor/MarkdownEditor.vue,
    src/components/editor/MetadataFields.vue,
    src/components/editor/ImageUploadZone.vue,
    src/components/editor/PreviewPane.vue,
    src/components/editor/TagsInput.vue,
    src/components/editor/StatusBadge.vue,
    src/router/index.ts
  </files>
  <action>
    PromptEditorView.vue — three modes from route:
    - Create mode: /prompts/new — empty draft (or recover from useDraftStore)
    - Edit mode: /prompts/:id/edit — load via usePromptDetail(id), pre-fill draftStore fields
    - Fork mode: /prompts/new?fork=:id — load source via usePromptDetail(fork id), pre-fill but clear author fields, set version to 1
    - On created: call draftStore.recover(promptId) to restore any saved draft for this prompt
    - On publish button click: validate (title required, content required, category required), call useCreatePrompt or useUpdatePrompt
    - Auth guard in router: /prompts/new and /prompts/:id/edit require isAuthenticated; if not, redirect to /browse

    EditorLayout.vue: ResizablePanelGroup with left panel (MarkdownEditor + MetadataFields + TagsInput) and right panel (PreviewPane). On mobile, shadcn Tabs for Write/Preview toggle (not split pane). Publish button in toolbar triggers parent's publish handler.

    MarkdownEditor.vue: shadcn Textarea with full height, monospace font. Emits 'update:modelValue'. Drag-and-drop handled by ImageUploadZone wrapper around the textarea. No markdown-it calls here — raw text only.

    MetadataFields.vue: shadcn Select components for content type (prompt/skill-file/skill-set), category (Engineering/Business/General), AI model (Claude/GPT-4/Gemini/Other), difficulty (beginner/intermediate/advanced). Text input for usage instructions (CONT-04, maps to frontmatter.usage_instructions). All fields emit updates to parent (PromptEditorView) which writes to useDraftStore.metadata.

    TagsInput.vue: Text input with tag chips. Enter key or comma adds a tag (max 5 tags, CONT-01). Each chip has an X dismiss button. Emits 'update:modelValue' with string[].

    ImageUploadZone.vue: Wraps its slot content (the textarea). Handles dragover/drop events and input[type=file] picker. On file selection: validate type and size client-side first (show error toast if invalid). Then POST multipart/form-data to `${import.meta.env.VITE_CF_WORKER_URL}/api/upload` with `Authorization: token ${authStore.token}`. On success: insert `![alt](url)` markdown link at cursor position in the editor. On CORS or 401 error: show toast "Image upload requires sign-in" or "Upload failed". Uses CONT-05 spec (max 10MB, PNG/JPG/GIF/WebP).

    PreviewPane.vue: Receives content: string prop. Renders via renderMarkdown(mdInstance, content) with 300ms debounce using VueUse useDebounceFn. Shows "Initializing preview..." spinner if !uiStore.markdownReady. v-html binding on output div with prose Tailwind classes.

    StatusBadge.vue: Shows "Saved {time}" when lastSaved is set, "Unsaved changes" when isDirty, "Saving..." during mutation. Uses useDraftStore.

    Router unsaved-changes guard: In src/router/index.ts beforeEach, after the auth check, add: `const draftStore = useDraftStore(); if (draftStore.isDirty && to.path !== from.path) { const confirmed = window.confirm('You have unsaved changes. Leave anyway?'); if (!confirmed) return false }`. Also add VueUse useEventListener(window, 'beforeunload', (e) => { if (draftStore.isDirty) { e.preventDefault(); e.returnValue = '' } }) in EditorLayout.vue (covers Pitfall 4 — browser back button).

    Route additions/confirmations in router/index.ts:
    - /prompts/new → PromptEditorView (meta: { requiresAuth: true })
    - /prompts/:id/edit → PromptEditorView (meta: { requiresAuth: true })
    - Ensure existing Phase 1 beforeEach guard still handles /admin route
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build succeeds. PromptEditorView renders the split-pane editor. MetadataFields shows all selects. TagsInput allows adding/removing tags. StatusBadge shows save state. PreviewPane renders markdown. ImageUploadZone accepts files. Router guard redirects unauthenticated users. All tests green.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npx vitest run src/stores/useDraftStore.spec.ts src/workers/upload.spec.ts` — pass
2. `npm run build` — zero TypeScript errors
3. `npx vitest run --coverage` — full suite green
4. Manual: Navigate to /prompts/new without auth → redirected to /browse. Sign in. Navigate to /prompts/new. Type in editor, see live preview. Trigger unsaved-changes guard by clicking nav link. Wait 30s, see "Saved" timestamp update. Upload an image (if R2 configured). Click publish to create GitHub Issue.
</verification>

<success_criteria>
- CONT-01/02/03/04: Create any content type (prompt/skill-file/skill-set) with all metadata fields including usage instructions
- CONT-05: Image upload validates type/size client-side; CF Worker validates and stores in R2; markdown link inserted
- CONT-06: PreviewPane shows live markdown preview with 300ms debounce
- CONT-07: Publish calls POST /repos/.../issues with YAML frontmatter body; issue created in data repo
- CONT-08: Edit mode loads existing prompt via usePromptDetail and pre-fills editor
- CONT-09: Fork mode loads source prompt via usePromptDetail with ?fork= query param; new issue on publish
- CONT-10: useDraftStore.save() called every 30s via useInterval; draft survives page reload via useLocalStorage
- CONT-11: Router beforeEach and beforeunload handler check isDirty before allowing navigation
- VERS-01: useUpdatePrompt.createVersionComment() posts version comment with ## Version N header
</success_criteria>

<output>
After completion, create `.planning/phases/02-read-and-contribute/02-02-SUMMARY.md` following the summary template.
</output>
