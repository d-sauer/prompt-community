import { describe, it } from 'vitest'

describe('router guards', () => {
  it.todo('requires auth: unauthenticated user redirected from /prompts/new to /browse')
  it.todo('requires maintainer: verifyMaintainerStatus called via GitHub API on /admin navigation (not just Pinia state)')
})
