import { describe, it } from 'vitest'

describe('useBookmarksStore', () => {
  it.todo('toggle adds and removes bookmark')
  it.todo('isBookmarked returns reactive boolean')
  it.todo('bookmarks persist across re-instantiation')
})
