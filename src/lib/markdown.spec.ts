import { describe, it } from 'vitest'

describe('markdown', () => {
  it.todo('initMarkdown returns MarkdownIt instance')
  it.todo('renderMarkdown renders code block with shiki class')
  it.todo('renders without XSS (html: false)')
})
