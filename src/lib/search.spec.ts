import { describe, it } from 'vitest'

describe('search', () => {
  it.todo('builds index from prompts array')
  it.todo('search returns results in < 50ms')
  it.todo('empty query returns all prompts')
  it.todo('search with no index returns empty array')
})
