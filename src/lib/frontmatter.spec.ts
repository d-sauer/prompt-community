import { describe, it } from 'vitest'

describe('frontmatter', () => {
  it.todo('buildFrontmatter round-trips through parseFrontmatter')
  it.todo('parseFrontmatter handles missing frontmatter')
  it.todo('tags with colons serialize correctly')
})
