import { describe, it } from 'vitest'

describe('useKeyboardShortcuts', () => {
  it.todo('⌘K sets commandPaletteOpen to true in useUIStore')
  it.todo('G+B navigates to /browse')
  it.todo('G+N navigates to /prompts/new')
  it.todo('shortcuts suppressed when input is focused')
  it.todo('[ toggles sidebarCollapsed')
})
