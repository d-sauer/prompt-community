import { describe, it } from 'vitest'

describe('useRestoreVersion', () => {
  it.todo('mutate posts new version comment with old content')
  it.todo('onSuccess invalidates prompt versions query')
})
