import { describe, it } from 'vitest'

describe('RestoreDialog', () => {
  it.todo('cancel button emits close without calling confirm')
  it.todo('confirm button calls onConfirm prop')
  it.todo('dialog shows version number and date in title')
})
