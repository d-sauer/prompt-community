import { describe, it } from 'vitest'

describe('NotificationDrawer', () => {
  it.todo('shows empty state when notificationCount is 0')
  it.todo('does not show badge when notificationCount is 0')
})
